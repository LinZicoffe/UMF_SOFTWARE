# UMF 超声波流量传感器 — 七点标定操作指南

## 1 概述

UMF 固件 v2.0 起支持七点分段线性插值标定，通过 Modbus RTU 逐步手动标定。

**标定原理**: 在量程范围内选取 7 个标定点，测量各点的实际流量与仪表显示流量的偏差，计算修正系数 k，固件在信号链中自动插值修正:

```
修正后流量 = 原始流量 × k(插值)
```

**目标精度**: ±1.5% FS（取决于传感器重复性和标定设备精度）。

---

## 2 前提条件

| 项目 | 要求 |
|------|------|
| 标准流量源 | 精度优于 ±0.5% 的标准流量计或称重法装置 |
| 通信接口 | RS-485 转 USB 适配器 |
| 上位机软件 | 任意 Modbus RTU 主站工具 (Modbus Poll / Simply Modbus / 本项目 modbus-debug skill) |
| 仪表参数 | 4mA 量程 (40031) 和 20mA 量程 (40032) 已正确配置 |
| 仪表系数 | 仪表系数 (40025~40026) 和介质系数 (40027~40028) 已正确配置 |

**默认通信参数**: 地址 02, 波特率 115200, 8N1

### 量程与标定范围的关系

标定的最大量程由 **4mA/20mA 量程参数** 决定，而非独立设置。信号链中，标定百分比的计算公式为:

```
标定百分比 = (当前流量 - 4mA量程值) / (20mA量程值 - 4mA量程值) × 100%
```

| 量程参数 | 4X 地址 | 类型 | 设置方式 |
|---------|---------|------|---------|
| 4mA 量程值 (量程下限) | 40031 | float | Modbus FC03/FC10 或 OLED 菜单 S14 |
| 20mA 量程值 (量程上限) | 40032 | float | Modbus FC03/FC10 或 OLED 菜单 S15 |

- 标定点 pct[6] = 100% 对应的流量 = **20mA 量程值**
- 标定点 pct[0] = 0% 对应的流量 = **4mA 量程值**
- 每个标定点的实际流量 = 4mA量程值 + (20mA量程值 - 4mA量程值) × pct%

**示例**: 若 4mA 量程 = 0, 20mA 量程 = 2000 L/h:
- pct[0]=0% → 0 L/h
- pct[1]=3% → 60 L/h
- pct[6]=100% → 2000 L/h

**重要**: 标定前必须先正确配置量程范围。修改量程后无需重新标定 k 值（k 是无量纲修正系数），
但如果实际运行流量超出了量程范围，超出部分将使用 k[6] 外推（不会停止工作，但精度下降）。

---

## 3 寄存器地址映射

### 3.1 标定控制

| 4X 地址 | 协议地址 (Hex) | 名称 | 类型 | 范围 | 默认值 | 读写 |
|---------|---------------|------|------|------|--------|------|
| 40096 | 0x005F | 标定使能 | uint16 | 0=禁用, 1=启用 | 0 | R/W |

### 3.2 标定修正系数 k[0]~k[6]

每个修正系数占用 2 个连续寄存器 (IEEE 754 float)。

| 4X 地址 | 协议地址 (Hex) | 名称 | 默认值 | 含义 |
|---------|---------------|------|--------|------|
| 40097~40098 | 0x0060~0x0061 | k[0] | 1.0 | 0% 量程点修正系数 |
| 40099~40100 | 0x0062~0x0063 | k[1] | 1.0 | 3% 量程点修正系数 |
| 40101~40102 | 0x0064~0x0065 | k[2] | 1.0 | 10% 量程点修正系数 |
| 40103~40104 | 0x0066~0x0067 | k[3] | 1.0 | 25% 量程点修正系数 |
| 40105~40106 | 0x0068~0x0069 | k[4] | 1.0 | 50% 量程点修正系数 |
| 40107~40108 | 0x006A~0x006B | k[5] | 1.0 | 75% 量程点修正系数 |
| 40109~40110 | 0x006C~0x006D | k[6] | 1.0 | 100% 量程点修正系数 |

**k 值范围**: 0.5 ~ 2.0

**k 值含义**: `k = 标准流量值 / 仪表显示流量值`
- k = 1.0 → 无修正
- k = 0.95 → 仪表偏高 5%, 修正后降低
- k = 1.05 → 仪表偏低 5%, 修正后提高

### 3.3 标定点百分比 pct[0]~pct[6]

每个标定点百分比占用 2 个连续寄存器 (IEEE 754 float)。

| 4X 地址 | 协议地址 (Hex) | 名称 | 默认值 (%) |
|---------|---------------|------|-----------|
| 40111~40112 | 0x006E~0x006F | pct[0] | 0.0 |
| 40113~40114 | 0x0070~0x0071 | pct[1] | 3.0 |
| 40115~40116 | 0x0072~0x0073 | pct[2] | 10.0 |
| 40117~40118 | 0x0074~0x0075 | pct[3] | 25.0 |
| 40119~40120 | 0x0076~0x0077 | pct[4] | 50.0 |
| 40121~40122 | 0x0078~0x0079 | pct[5] | 75.0 |
| 40123~40124 | 0x007A~0x007B | pct[6] | 100.0 |

**pct 值范围**: 0.0 ~ 100.0

> 标定点百分比可自定义。例如传感器在某段非线性严重，可在该区间加密标定点。
> 百分比必须递增，否则插值结果不可预测。

---

## 4 Float 寄存器读写规则

### 4.1 FC06 写入 Float (分次写入)

Float 占 2 个寄存器，通过两次 FC06 写入:

1. **第一次** (写低字寄存器): 缓存低 16 位，不触发保存
2. **第二次** (写高字寄存器): 写入高 16 位 **并触发保存**

**示例**: 写入 k[0] = 0.95 (IEEE 754: 0x3F733333)

```
第一次 FC06: 写低字 → 协议地址 0x0060, 数据 0x3333
  发送: 02 06 00 60 33 33 [CRC_L] [CRC_H]

第二次 FC06: 写高字 → 协议地址 0x0061, 数据 0x3F73
  发送: 02 06 00 61 3F 73 [CRC_L] [CRC_H]
  (此帧触发 Flash 持久化)
```

### 4.2 FC16 批量写入 Float

FC16 可一次写入完整 float (2 个寄存器):

```
FC16: 写 k[0] → 起始地址 0x0060, 寄存器数 2, 字节数 4
  发送: 02 10 00 60 00 02 04 33 33 3F 73 [CRC_L] [CRC_H]
```

### 4.3 FC03 读取 Float

读取 2 个连续寄存器即可获得完整 float:

```
FC03: 读 k[0] → 起始地址 0x0060, 寄存器数 2
  发送: 02 03 00 60 00 02 [CRC_L] [CRC_H]
  响应: 02 03 04 33 33 3F 73 [CRC_L] [CRC_H]
         (低字)     (高字)
```

---

## 5 常用 IEEE 754 Float 编码参考

| 数值 | IEEE 754 Hex | 低字 | 高字 |
|------|-------------|------|------|
| 0.0 | 0x00000000 | 0x0000 | 0x0000 |
| 0.5 | 0x3F000000 | 0x0000 | 0x3F00 |
| 0.9 | 0x3F666666 | 0x6666 | 0x3F66 |
| 0.95 | 0x3F733333 | 0x3333 | 0x3F73 |
| 1.0 | 0x3F800000 | 0x0000 | 0x3F80 |
| 1.05 | 0x3F866666 | 0x6666 | 0x3F86 |
| 1.1 | 0x3F8CCCCD | 0xCCCD | 0x3F8C |
| 1.2 | 0x3F99999A | 0x999A | 0x3F99 |
| 1.5 | 0x3FC00000 | 0x0000 | 0x3FC0 |
| 2.0 | 0x40000000 | 0x0000 | 0x4000 |
| 3.0 | 0x40400000 | 0x0000 | 0x4040 |
| 10.0 | 0x41200000 | 0x0000 | 0x4120 |
| 25.0 | 0x41C80000 | 0x0000 | 0x41C8 |
| 50.0 | 0x42480000 | 0x0000 | 0x4248 |
| 75.0 | 0x42960000 | 0x0000 | 0x4296 |
| 100.0 | 0x42C80000 | 0x0000 | 0x42C8 |

---

## 6 标定操作流程

### 6.1 标定前准备

**步骤 1**: 确认标定当前处于禁用状态

```
FC03: 读标定使能 → 地址 0x005F (40096), 数量 1
  发送: 02 03 00 5F 00 01 [CRC]
  期望响应: ... 00 00 ...  (值为 0 = 禁用)
```

**步骤 2**: 读取当前量程范围 (确认 4mA 和 20mA 对应的流量值)

```
FC03: 读量程值 → 地址 0x001E (40031), 数量 2 (连续读 4mA 和 20mA)
  发送: 02 03 00 1E 00 02 [CRC]
  响应: 02 03 04 [4mA低字L] [4mA低字H] [20mA低字L] [20mA低字H] [CRC]
```

> 量程 = 20mA值 - 4mA值。标定点的流量 = 4mA值 + 量程 × pct%

> 量程 = 20mA值 - 4mA值。标定点的流量 = 4mA值 + 量程 × pct%

**步骤 3**: (可选) 如需自定义标定点百分比，先写入 pct 寄存器

使用 FC06 或 FC16 写入 pct[0]~pct[6]。一般使用默认值 [0, 3, 10, 25, 50, 75, 100] 即可。

### 6.2 逐步标定

以下假设量程为 0~2000 L/h (4mA=0, 20mA=2000)。

#### 标定点 0: pct[0] = 0% → 流量 = 0 L/h (零点)

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 关闭流量 | 标准流量源输出 0 |
| 2 | 等待读数稳定 | 至少等待 3 个阻尼周期 |
| 3 | 记录仪表显示流量 | 例如: 5.2 L/h |
| 4 | 计算 k[0] | 标准值 / 仪表值 = 0 / 5.2 = 0.0 |
| 5 | 写入 k[0] | 见下方指令 |

> 零点偏差通常不通过 k 修正 (k=0 会导致所有流量归零)。
> 零点建议设 k[0] = 1.0 (不修正), 通过小信号切除功能处理。

```
FC16: 写入 k[0] = 1.0 → 起始地址 0x0060, 寄存器数 2
  发送: 02 10 00 60 00 02 04 00 00 3F 80 [CRC_L] [CRC_H]
```

#### 标定点 1: pct[1] = 3% → 流量 = 60 L/h

| 步骤 | 操作 |
|------|------|
| 1 | 标准流量源输出 60 L/h |
| 2 | 等待读数稳定 |
| 3 | 记录仪表显示流量 (例: 63 L/h) |
| 4 | 计算 k[1] = 60 / 63 = 0.952 |

```
FC16: 写入 k[1] = 0.952 → 起始地址 0x0062, 寄存器数 2
  (0.952 的 IEEE 754: 0x3F73F7AE)
  发送: 02 10 00 62 00 02 04 F7 AE 3F 73 [CRC_L] [CRC_H]
```

> 实际操作中建议使用上位机软件自动计算 IEEE 754 编码。

#### 标定点 2~6: 重复上述步骤

| 标定点 | pct | 流量 (L/h) | 操作 |
|--------|-----|-----------|------|
| 2 (k[2]) | 10% | 200 | 调节标准源 → 记录 → 计算 k → 写入地址 0x0064 |
| 3 (k[3]) | 25% | 500 | 调节标准源 → 记录 → 计算 k → 写入地址 0x0066 |
| 4 (k[4]) | 50% | 1000 | 调节标准源 → 记录 → 计算 k → 写入地址 0x0068 |
| 5 (k[5]) | 75% | 1500 | 调节标准源 → 记录 → 计算 k → 写入地址 0x006A |
| 6 (k[6]) | 100% | 2000 | 调节标准源 → 记录 → 计算 k → 写入地址 0x006C |

**k 值计算公式**:
```
k[i] = 标准流量源读数 / 仪表显示流量读数
```

**各标定点 k 寄存器写入地址 (FC16)**:

| 标定点 | 起始协议地址 | FC16 起始地址字段 |
|--------|------------|-----------------|
| k[0] | 0x0060 | `02 10 00 60 00 02 04 [低字L] [低字H] [高字L] [高字H] [CRC]` |
| k[1] | 0x0062 | `02 10 00 62 00 02 04 [低字L] [低字H] [高字L] [高字H] [CRC]` |
| k[2] | 0x0064 | `02 10 00 64 00 02 04 [低字L] [低字H] [高字L] [高字H] [CRC]` |
| k[3] | 0x0066 | `02 10 00 66 00 02 04 [低字L] [低字H] [高字L] [高字H] [CRC]` |
| k[4] | 0x0068 | `02 10 00 68 00 02 04 [低字L] [低字H] [高字L] [高字H] [CRC]` |
| k[5] | 0x006A | `02 10 00 6A 00 02 04 [低字L] [低字H] [高字L] [高字H] [CRC]` |
| k[6] | 0x006C | `02 10 00 6C 00 02 04 [低字L] [低字H] [高字L] [高字H] [CRC]` |

### 6.3 启用标定

所有 k 值写入完成后，启用标定:

```
FC06: 写标定使能 = 1 → 地址 0x005F, 数据 0x0001
  发送: 02 06 00 5F 00 01 [CRC_L] [CRC_H]
```

> 启用后信号链立即生效: corrected_flow = raw_flow × k(插值)

### 6.4 验证标定结果

**步骤 1**: 依次调节标准流量源到各标定点流量

**步骤 2**: 读取仪表显示流量 (通过 Modbus 或 OLED)

```
FC03: 读瞬时流量 → 地址 0x0000 (40001), 数量 2
  发送: 02 03 00 00 00 02 [CRC]
```

**步骤 3**: 计算误差

```
误差 (%) = (仪表读数 - 标准值) / 量程 × 100
```

目标: 所有标定点误差 ≤ ±1.5% FS

### 6.5 持久化确认

标定参数在每次 setter 调用时自动写入 Flash (Page 61)，断电不丢失。

验证方法:
1. 断电重启仪表
2. 读取 k[0]~k[6] 确认值未丢失
3. 读取标定使能确认仍为 1

```
FC03: 批量读取全部标定参数 → 地址 0x005F, 数量 29 (覆盖 95~123)
  发送: 02 03 00 5F 00 1D [CRC]
```

---

## 7 完整标定示例

以下是一个完整的标定过程示例，假设量程 0~2000 L/h:

| 标定点 | 标准流量 (L/h) | 仪表读数 (L/h) | k 值 |
|--------|---------------|---------------|------|
| 0% | 0 | 0 | 1.000 (不修正) |
| 3% | 60 | 63 | 0.952 |
| 10% | 200 | 206 | 0.971 |
| 25% | 500 | 510 | 0.980 |
| 50% | 1000 | 1015 | 0.985 |
| 75% | 1500 | 1510 | 0.993 |
| 100% | 2000 | 1990 | 1.005 |

### 写入全部 k 值 (FC06 逐个写入)

```
-- k[0] = 1.0 (地址 96~97)
TX: 02 06 00 60 00 00 [CRC]    -- 低字
TX: 02 06 00 61 3F 80 [CRC]    -- 高字, 触发保存

-- k[1] = 0.952 (地址 98~99)
TX: 02 06 00 62 F7 AE [CRC]    -- 低字
TX: 02 06 00 63 3F 73 [CRC]    -- 高字, 触发保存

-- k[2] = 0.971 (地址 100~101)
TX: 02 06 00 64 1E B8 [CRC]    -- 低字 (0x1EB8 近似)
TX: 02 06 00 65 3F 78 [CRC]    -- 高字, 触发保存

-- ... 以此类推 ...
```

> 实际操作建议使用上位机软件 (如 Modbus Poll) 直接输入 float 数值，
> 软件自动完成 IEEE 754 编码和分次写入。

### 启用标定

```
TX: 02 06 00 5F 00 01 [CRC]
RX: 02 06 00 5F 00 01 [CRC]    -- 回显确认
```

---

## 8 禁用标定 (恢复未标定状态)

```
FC06: 写标定使能 = 0 → 地址 0x005F, 数据 0x0000
  发送: 02 06 00 5F 00 00 [CRC_L] [CRC_H]
```

禁用后信号链跳过插值修正，恢复原始流量值。k 值保留在 Flash 中，随时可重新启用。

---

## 9 恢复标定默认值

如需清除所有标定数据:

```
-- 方法 1: 逐个写入 k[i] = 1.0 (7 次 FC16 写入)
02 10 00 60 00 02 04 00 00 3F 80 [CRC]   -- k[0]=1.0
02 10 00 62 00 02 04 00 00 3F 80 [CRC]   -- k[1]=1.0
02 10 00 64 00 02 04 00 00 3F 80 [CRC]   -- k[2]=1.0
02 10 00 66 00 02 04 00 00 3F 80 [CRC]   -- k[3]=1.0
02 10 00 68 00 02 04 00 00 3F 80 [CRC]   -- k[4]=1.0
02 10 00 6A 00 02 04 00 00 3F 80 [CRC]   -- k[5]=1.0
02 10 00 6C 00 02 04 00 00 3F 80 [CRC]   -- k[6]=1.0

-- 禁用标定使能
02 06 00 5F 00 00 [CRC]
```

> 也可以通过菜单 "Calibration → Reset Defaults" 操作 (如已实现)。

---

## 10 注意事项

1. **标定顺序**: 建议从低流量到高流量逐点标定，每个点等待读数稳定后再记录
2. **温度影响**: 标定应在工作温度下进行，温度变化会影响传感器精度
3. **k 值范围**: 0.5 ~ 2.0，超出范围会被 clamp。如果 k 值偏离 1.0 超过 20%，建议先检查仪表系数和介质系数
4. **百分比递增**: pct[0] < pct[1] < ... < pct[6]，违反此条件会导致插值异常
5. **写入次数**: Flash Page 61 支持 14 次写入周期 (标定+仪表系数共享)，出厂标定和年检场景绑绑有余，**不要在自动化程序中频繁写入**
6. **标定使能**: 写入 k 值时不需要先启用标定，可以先写入所有 k 值，验证无误后再启用
7. **断电安全**: 每次 k 值写入都会触发 Flash 持久化，确保断电不丢失
