<Window x:Class="UMF流量计.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UMF流量计"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:views="clr-namespace:UMF流量计.Views"
        xmlns:cvt="clr-namespace:UMF流量计.Converters"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="750" Width="1200"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <cvt:IndexToVisibilityConverter x:Key="IndexToVisibilityConverter"/>
        <cvt:ConnectionStatusBrushConverter x:Key="ConnectionStatusBrushConverter"/>
        <cvt:FloatToStringConverter x:Key="FloatToStringConverter"/>
        <cvt:ULongToStringConverter x:Key="ULongToStringConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border Grid.Row="0" Background="{DynamicResource PrimaryBrush}" Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Title}" Foreground="White"
                           FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="12" Height="12" Margin="0,0,8,0"
                             Fill="{Binding IsConnected, Converter={StaticResource ConnectionStatusBrushConverter}}"/>
                    <TextBlock Text="{Binding StatusText}" Foreground="White" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧导航栏 -->
            <Border Grid.Column="0" Background="{DynamicResource SecondaryRegionBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
                <ListBox ItemsSource="{Binding MenuItems}" SelectedIndex="{Binding SelectedNavigationIndex}"
                         BorderThickness="0" Background="Transparent">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="12,8">
                                <TextBlock Text="{Binding Icon}" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding Title}" FontSize="14" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>

            <!-- 右侧内容区 -->
            <Grid Grid.Column="1" Margin="8">
                <views:ConnectionView Visibility="{Binding SelectedNavigationIndex, Converter={StaticResource IndexToVisibilityConverter}, ConverterParameter=0}"/>
                <views:DashboardView Visibility="{Binding SelectedNavigationIndex, Converter={StaticResource IndexToVisibilityConverter}, ConverterParameter=1}"/>
                <views:TrendView Visibility="{Binding SelectedNavigationIndex, Converter={StaticResource IndexToVisibilityConverter}, ConverterParameter=2}"/>
                <views:CalibrationView Visibility="{Binding SelectedNavigationIndex, Converter={StaticResource IndexToVisibilityConverter}, ConverterParameter=3}"/>
                <views:ConfigView Visibility="{Binding SelectedNavigationIndex, Converter={StaticResource IndexToVisibilityConverter}, ConverterParameter=4}"/>
                <views:ControlView Visibility="{Binding SelectedNavigationIndex, Converter={StaticResource IndexToVisibilityConverter}, ConverterParameter=5}"/>
                <views:SimulationView Visibility="{Binding SelectedNavigationIndex, Converter={StaticResource IndexToVisibilityConverter}, ConverterParameter=6}"/>
            </Grid>
        </Grid>

        <!-- 底部状态栏 -->
        <Border Grid.Row="2" Background="{DynamicResource SecondaryRegionBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0"
                Padding="16,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding ConnectionInfo}"
                           FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"
                           VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1"
                           Text="{Binding LatestSensorData.Timestamp, StringFormat='最后更新: {0:HH:mm:ss}'}"
                           FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

    </Grid>
</Window>
