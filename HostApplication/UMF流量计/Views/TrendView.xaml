<UserControl x:Class="UMF流量计.Views.TrendView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="趋势图表" FontSize="24" FontWeight="Bold" Margin="0,0,0,16"/>

        <!-- 工具栏 -->
        <hc:ButtonGroup Grid.Row="1" Margin="0,0,0,12">
            <ToggleButton Content="暂停采集" IsChecked="{Binding TrendVm.IsPaused}"
                          Command="{Binding TrendVm.TogglePauseCommand}"/>
            <Button Content="清除数据" Command="{Binding TrendVm.ClearDataCommand}"/>
        </hc:ButtonGroup>

        <!-- 流量趋势 -->
        <Border Grid.Row="2" Background="{DynamicResource RegionBrush}" CornerRadius="8" Padding="12"
                Effect="{DynamicResource EffectShadow2}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="瞬时流量趋势" FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>
                <ListView ItemsSource="{Binding TrendVm.FlowRatePoints}" AlternationCount="2">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="时间" Width="150"
                                            DisplayMemberBinding="{Binding Time, StringFormat='HH:mm:ss'}"/>
                            <GridViewColumn Header="流量 (m³/h)" Width="150"
                                            DisplayMemberBinding="{Binding Value, StringFormat=F2}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </DockPanel>
        </Border>

        <!-- 底部状态 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12">
                <TextBlock.Text>
                    <MultiBinding StringFormat="数据点: {0}">
                        <Binding Path="TrendVm.FlowRatePoints.Count"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
