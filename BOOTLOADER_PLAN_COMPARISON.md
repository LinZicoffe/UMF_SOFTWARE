# 两份 Bootloader 方案对比评审

| 项 | 内容 |
|----|------|
| 被比较对象 | **方案 A**：`UMF_Bootloader_Upgrade_Plan.md`（本仓库，三轮三方独立审查，最终三方 PASS）<br>**方案 B**：`DESIGN_bootloader_upgrade.md`（另一 AI 产出，自称一轮审查 PASS WITH CHANGES） |
| 评审方式 | 逐条把两份方案的**具体主张**与 `BSP/`、`Core/`、`EWARM/` 源码及 `UMF.map` 实测数据核对 |
| 结论 | **方案 A 整体更可靠（可落地性），但方案 B 有两处设计明显优于方案 A，应当吸收** → 建议做**合并版 v3**，见 §6 |

---

## 0. 结论速览

1. **方案 B 有一个会直接导致"升级后设备死机 + 复位循环"的确定性缺陷**：跳转序列 `__set_PRIMASK(1)` 后**没有重新使能中断**，而 App 自身从不使能中断（详见 §4.1）。这不是风格问题，是必现故障。
2. **方案 B 有一处事实性错误**：IWDG "系统复位会停止" 不成立（§4.2）——而它正是靠这个前提决定"BL 不启动 IWDG"。
3. **方案 B 的升级通道是最大工程风险**：USART1/PA9-PA10 接的是 UFL-1A 模块，是否引出到接插件未确认，且存在与模组 TX 争用 PA10 的风险（方案 B 自己在 §12-1 承认，却在 §5.1 断言"无干扰"）。**而方案 A 用 RS-485/USART2，是客户现成在用的对外总线，确定可用。**
4. **方案 B 的两处设计确实更好，值得吸收**：
   - **固件头写在 App 镜像内（最后写）** —— 不占额外 Flash 页，且"有效性标记随 App 区一起被擦除"，天然没有"陈旧 VALID"问题；比方案 A 的独立描述符页更优雅。
   - **EEPROM 固定偏移 + 整页 RMW** —— 实现复杂度比方案 A 的记录型日志 + GC 低一个数量级（而日志/GC 正是方案 A 最复杂、三轮审查里改动最多的部分）。
5. 方案 A 另有三处是经过对抗性审查才定下来的、方案 B 缺失的内容：参数迁移不依赖人工、跳转序列、IWDG 真实周期分析。

---

## 1. 对比总表

| 维度 | 方案 A | 方案 B | 判定 |
|------|--------|--------|------|
| 分区 | BL 6KB(页0~5) + 备份页(6) + 描述符页(7) + App 53KB(页8~60) + 参数 3KB | BL 4KB(页0~3) + App 57KB(页4~60) + 参数 3KB | **B 空间更好**（App 余量 12.2KB vs 8.1KB），但 B 的 BL 预算 2.6~2.8KB 偏乐观，4KB 门禁若破则全盘重排 |
| 升级通道 | RS-485 / USART2（Modbus IAP） | USART1 / PA9-PA10（XModem-CRC） | **A 明显更优**（B 通道是否物理可达未确认 + 与模组共线风险） |
| 协议复杂度 | 自定义 Modbus IAP（242B/帧，ADU 上限、流控、超时需自研） | XModem-CRC（成熟、极简） | **B 略优**（BL 与工具都更简单） |
| 防砖标记 | 独立描述符页 + **擦除前先写 INVALID** + 冷启动整镜像 CRC | **固件头写在 App 内、最后写** | **B 更优雅**（不占页；有效性标记随被擦区域一起消失，无陈旧 VALID；A 的描述符页还需每 25 次升级擦一次页） |
| 参数迁移 | **BL 自动备份旧 10 页 → App 首次迁移，零丢失、无需人工** | 依赖上位机手工备份/恢复（页 54~60 配置丢失）；页 61~63 可自动迁移 | **A 明显更优**（200+ 台现场，"记得先备份"是操作风险；B 自己也把"是否接受首次升级丢参数"列为待决项 §12-5） |
| EEPROM 重构 | 记录型日志 + 三页环形 GC（~3.8×10⁵ 次写入，记录级 CRC，掉电原子） | 固定偏移 + 整页 RMW（每次保存擦整页；擦后写回窗口丢整页） | **实现难度 B 明显更优**；**掉电/磨损 A 更优**（B 把"页级提交标记"延后为 YAGNI，但该窗口会丢整页标定） |
| 跳转序列 | 关中断 → 停 SysTick → 清 ICER/ICPR → **清 SCB->ICSR(SysTick/PendSV 挂起)** → 校验向量表 → 放宽看门狗 → 时钟 deinit → 设 VTOR → 设 MSP → **`__enable_irq()`** → 跳转 | `__set_PRIMASK(1)` → 清 NVIC 挂起 → 设 VTOR → 设 MSP → 跳转（**无 `__enable_irq()`**，无 ICSR 清挂起，无时钟 deinit） | **A 明显更优**（B 会必现死机，见 §4.1） |
| IWDG | 实测 **约 100 ms**（`IWDG_PRESCALER_4` 枚举值即 0x00）；会话期改 5s；暖复位后仍在运行 | "默认选项字节下系统复位会停止 IWDG"（**错误**）+ 防御性喂狗 | **A**（B 结论错误，但防御性喂狗部分兜住后果） |
| 主机侧 | 集成进现有 .NET WPF（+3~4 人天，客户体验好） | Python 脚本（复用已有 skill 生态，更快、更易版本管理） | **B 成本更低**，A 集成度更好 |
| 文档 | 768 行：地址总表、协议状态机、错误码、38 条测试用例、FMEA、风险表、审查记录 | 356 行：结构清晰、阶段划分明确、有"明确不做(YAGNI)"与"前置确认项" | 各有优点（B 更好读，A 更可实施） |

---

## 2. 方案 B 应被吸收的两个设计（客观更好）

### 2.1 固件头写在 App 镜像内、最后写
方案 B 把 24B 固件头放在 `App + 0x200`（在 App 链接脚本里保留一个 `.fw_header` section），升级时**最后一步才写头** ⇒ 头存在即代表全部数据页已写完。

这比方案 A 的独立描述符页好在三点：
- **不额外占用 1KB 页**（方案 A 的描述符页只放得下 24 个 32B 描述符，且还需每隔 25 次升级擦页）；
- **不存在"陈旧 VALID"**：有效性标记位于被擦除的 App 区内，擦除动作本身就使其失效；方案 A 必须靠"擦除前先追加 INVALID"这条额外纪律来弥补；
- **没有描述符页擦除窗口**（方案 A 在该窗口内掉电会丢失描述符）。

方案 A 需要补的：① 校验层次（B 只查 magic + img_size，方案 B 的 §5.6 后备校验用的是"向量表健全性"，正是方案 A 审查中被判定为过弱的做法）；② BL 的通信参数来源（B 用固定 115200 无地址依赖，方案 A 改用头内字段是编译期值，仍需运行期来源）。

### 2.2 EEPROM 固定偏移 + 整页 RMW
方案 B 用"每页固定偏移分组 + 整页读改写"，取代现有 append-log。**实现与验证成本比方案 A 的记录型日志低一个数量级** —— 方案 A 的日志/GC/压实恢复是三轮审查中改动最多、最易出错的部分（死页、恒真触发、半擦页判定、停止/跳过语义等）。

**但必须补方案 B 自己延后的那条**：RMW 的"擦页 → 写回"窗口内掉电会丢失该页**全部**分组（对 Page 61 就是 meter_coeff + 全部七点标定）。方案 B 说"YAGNI，P4 再评估"，但这是**丢标定**级后果，建议直接采用页级提交标记（第 0 字写页提交号 + 读时校验），成本约几十字节代码。

---

## 3. 方案 A 更可靠的三处（经审查固化）

1. **参数迁移不依赖人工**（§1 表已述）。方案 B §4.4 要求"全部 10 组参数在首次升级时都依赖上位机备份"，且 §12-5 直接问用户"是否接受首次升级丢参数"——对 200+ 台存量设备，这是把工程风险转成了现场操作风险。
   另注：方案 B §4.3（App 内实现旧格式启动迁移）与 §4.4（"全部 10 组都依赖上位机备份"）**自相矛盾**：既然 §4.3 实现了旧 append-log 读取器，则物理存活的页 61~63（含七点标定）本可自动迁移，不必依赖备份。
2. **升级通道确定可用**（RS-485/USART2 是客户现成对外总线；USART1/PA9-PA10 是否引出、是否与模组共线均未确认）。
3. **跳转与看门狗正确**（见 §4）。

---

## 4. 方案 B 的确定性缺陷（含证据）

### 4.1 【致命】跳转后 App 中断全程屏蔽 → HAL_Delay 死锁 → 复位循环

方案 B §5.6 的跳转代码：
```c
__set_PRIMASK(1);                     /* 关全局中断 */
NVIC 清全部挂起;
SCB->VTOR = APP_BASE;
__set_MSP(*(uint32_t*)APP_BASE);
(*(void(**)(void))(APP_BASE + 4))();  /* 跳转，不再返回 —— 但没有恢复 PRIMASK */
```
`PRIMASK` 是内核寄存器，**不随跳转清零**；证据链：

| 证据 | 位置 |
|------|------|
| App 全文**没有** `__enable_irq()`（唯一出现处是 ssd1306 的"保存-恢复"惯用法 `if (!primask) __enable_irq();`，PRIMASK=1 时不会打开） | `Core/Src/main.c`（仅 `:380` `__disable_irq()` 在 `Error_Handler`）、`OLED/ssd1306.c:69-75,82-90` |
| `HAL_Init()` 不使能全局中断 | `Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal.c:142` |
| IAR 启动文件不触碰 PRIMASK | `EWARM/startup_stm32f103xb.s`（无 `CPSIE`） |
| App 启动期就调用依赖 SysTick 的 `HAL_Delay` | `OLED/ssd1306.c:60,62,103,105,236`（`HAL_Delay(10)`/`HAL_Delay(100)`） |

⇒ 后果：跳转后 App 在 OLED 初始化处**永久卡死**；由于 IWDG 约 100 ms 仍在运行且无人喂狗 ⇒ 复位 ⇒ BL 校验固件头有效 ⇒ 再跳转 ⇒ 再卡死 ⇒ **无限复位循环，设备表现为"砖"**（实际镜像完好，只是 App 起不来），需 SWD 才能恢复。

这与方案 A 第一轮审查发现的 B1 是**同一个缺陷**，方案 A 已修（新增 `__enable_irq()` + `__disable_irq` 前清 `SCB->ICSR` 的 SysTick/PendSV 挂起位）。方案 B 的"一轮审查已 PASS"并未覆盖此项。

### 4.2 【事实错误】"系统复位会停止 IWDG"
方案 B §5.8 以"默认选项字节下系统复位会停止 IWDG"为前提决定 BL 不启动 IWDG。但 STM32F1 的 IWDG 一旦启动**只有上电复位/写选项字节才能停止**，`NVIC_SystemReset()`（方案 B §5.3 的升级触发方式）之后它仍在运行。

补充实测：本工程 IWDG 为 `IWDG_PRESCALER_4`（**枚举值 0x00 ⇒ 分频 4**）+ Reload 999 ⇒ **约 100 ms**（`Core/Src/iwdg.c:41-42`、`stm32f1xx_hal_iwdg.h:79`）——比方案 B 隐含假设的"~1s"紧 10 倍。方案 B 的防御性喂狗方向正确，但缺少"会话期放宽周期 + 跳转前放宽以覆盖 App 启动期"这两步，且分析前提错误。

### 4.3 【清单漏项】标定备份范围少 1 个寄存器
方案 B §4.4 写"标定使能/系数/百分比 40096~**40123**"。实测（`BSP/bsp_usart.h:112-127`，0 基 PDU）：
`CalEnableAddr=95`、`CalPct6Addr=122`、`CalEndAddr=123` ⇒ 4x 范围是 **40096~40124**。`cal_pct[6]`（100% 标定点）占 PDU 122/123，即 4x 40123/40124。
⇒ 按方案 B 的表，`cal_pct[6]` 的**高字（40124）不会被备份/恢复**；又因方案 B 自己规定"float 须低字先行、高字到达才触发 setter"，该点会**静默退回默认值**。这正是"标定数据"类错误。（方案 B 的验收条款"逐寄存器回读比对一致"有可能把它抓出来，但范围表本身是错的。）

### 4.4 【自相矛盾】迁移责任归属
§4.3 说"必须实现启动迁移，不能只靠上位机备份"，§4.4 又说"全部 10 组参数在首次升级时都依赖上位机备份"，§12-5 再问用户"是否接受首次升级丢参数"。三者需统一。

### 4.5 【预算乐观】BL 4KB 门禁
方案 B §5.2 自估 2.6~2.8KB。独立估算（本方案 §4.1 与审查员 R1 各自独立得出）寄存器级 BL 为 **4.5~5.5KB**；差异主要来自方案 B 未计入：XModem 所需的 CRC16（只列了 CRC32）、跨页擦除位图、超时/重试边界、以及"只允许写 Page 4~60"的边界检查代码。若实测超 4KB，按方案 B 自己的预案需"重走分区评审"。

---

## 5. 决定性的未决硬件前提

方案 B 的升级通道成立与否取决于一个 5 分钟可查的硬件问题（方案 B §12-1 已列为前置确认项 #1）：

1. **PA9/PA10 是否引到接插件**？若没有，就必须开壳焊接 ⇒ "无需拆机"目标失效，方案 B 的主要卖点不成立。
2. **PA10 是否与 UFL-1A 的 TX 共线**？若共线，需确认 BL 期间模组是否真的静默。注意本工程 `FlowPassiveReadCmdEnable`（`BSP/bsp_usart.c:143` 注释"模组被动发送数据"）在启动时被置位，**模组可能处于周期性主动上报状态**；MCU 复位进 BL 后模组仍在运行，其 TX 会与升级工具的 TX 在 PA10 上争用。

只要 1 或 2 不成立，方案 B 的 XModem/USART1 通道即不可用，而方案 A 的 RS-485/USART2 不受影响。

---

## 6. 建议：合并为 v3（取两家之长）

| 组成 | 取自 | 说明 |
|------|------|------|
| 固件头写在 App 内、最后写 | **B** | 省下描述符页；消除陈旧 VALID 与擦页窗口 |
| 校验层次 | **A** | B 只查 magic+size 过弱；改为"头 + 向量表"每次上电，整镜像 CRC32 在 `CMD/激活` 与冷启动时执行 |
| EEPROM 固定偏移 + RMW | **B** | 大幅降低 M1 实现风险 |
| 页级提交标记 | **A 要求补** | 堵住 RMW 擦写窗口丢整页（含七点标定）的路径 |
| 升级通道 | **A** | RS-485/USART2；除非硬件确认 PA9/PA10 已引出且无争用 |
| 旧参数自动备份页 | **A** | BL 首次会话即备份旧 10 页，现场零人工 |
| 跳转序列 + IWDG 处理 | **A** | 已修 `__enable_irq`/ICSR/时钟 deinit/看门狗放宽；IWDG 实为 100 ms |
| 上位机工具 | **B（可合并）** | Python 脚本先落地验证协议，WPF 升级页作为后续客户界面 |
| 测试与验收 | **A 为主 + B 补充** | A 的 T-01~T-38；并入 B 的"备份/恢复逐寄存器比对一致""按段擦除不伤 BL""选项字节场景演练" |

**合并后可再省 1KB**：BL 页 0~5（6KB）｜旧参数备份页 6｜App 页 **7~60**（`0x08001C00~0x0800F3FF` = 55,296 B，余量 **9,128 B**）｜参数页 61~63。若 BL 实测 ≤3.5KB，还可压缩为 BL 页 0~3 + 备份页 4 + App 页 5~60（56KB）。

---

## 7. 两份方案共有的待核实项（与选型无关，建议一并处理）

1. **Span 的 Modbus 写入路径疑似不完整**：`Core/Inc/main.h:105-106` 定义 `SpanValueStartMaxAddress = 32`（PDU），而 2 个 float 需覆盖 PDU 30~33；`bsp_usart.c:1478-1490` 每次迭代只写 `.str[1]/.str[0]`。**两份方案的"Span 备份/恢复"都会踩到**，建议 P0/M1 一并核实修正（方案 B 把"含 SpanHi"写进验收条款是有价值的做法）。
2. **陈旧副本**：`Drivers/CMSIS/Src/main.c`、`Drivers/CMSIS/Inc/main.h` 不参与编译但含旧页地址常量 —— 方案 B 指出应在迁移时排除误扫，**这是个好发现**，双方都应采纳（删除或标注废弃）。
3. `CLAUDE.md` 中"Flash 已用 ~34KB""IWDG 约 1 秒"均为过时/错误数据，两份方案都应同步修正。

---

*本对比中所有源码引用均已逐条打开核对；方案 A 的相关结论来自其第一~三轮三方独立审查记录（`UMF_Bootloader_Upgrade_Plan.md` 文首修订清单）。*
