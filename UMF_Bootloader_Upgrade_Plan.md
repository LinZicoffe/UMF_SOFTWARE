# UMF 超声波流量计 Bootloader（IAP 在线升级）方案 v3.2

| 项目 | 内容 |
|------|------|
| 文档版本 | **v3.2 —— 已通过三方独立审查（R1 内存布局/跳转、R2 存储/掉电安全、R3 协议/可行性），最终均判 PASS（可交付）** |
| 审查历程 | v1：19 条 Blocker（FAIL/PASS_WITH_CHANGES×2）→ v2.0：25 条收口项 → v3.0：吸收对比方案的两处更优设计并重构（11 条）→ v3.1：WRP 结论反转等 15 项 → **v3.2：6 项文字/口径收口（0 条设计级问题）** |
| 目标硬件 | STM32F103C8T6，64KB Flash（页 1KB）/ 20KB RAM，无外部 Flash，**不可换芯片** |
| 构建 | IAR EWARM 8.32（`EWARM/UMF.ewp`），裸机超级循环 |
| 结论 | **可行（有条件）** —— 但**第一步必须先回答 §2.6 的业务前提**，否则不该投入 |

> **v3.0 相对 v2.0 的重构**（不是文字修订，是设计变更）：
> 1. **采纳方案 B 的"固件头内嵌 App 镜像、最后写"** 取代 v2 的独立描述符页 —— 省下 1KB 页、天然无"陈旧 VALID"、无描述符页擦除窗口。
> 2. **取消记录型日志与 GC**，取消 **BL 侧的新存储扫描器**（v2 的 400 B 复杂度点，位于"失联即变砖"链条上）；EEPROM 改为 **3 页轮转 + 每页完整定长镜像 + seq/CRC**，BL 只读一个 **16 B 固定槽位**。
> 3. **传输改为 XModem-CRC（1K/128B）over RS-485** —— 取消 Modbus 从站地址依赖（XModem 是点对点，无需地址），**不可用窗口由 20~30 s 缩短到 8~10 s**。
> 4. 分区调整：App 由 53KB 增至 **54KB（页 7~60，`0x08001C00~0x0800F3FF`）**，余量 9,128 B。
> 5. 空闲窗口 60 s → **15 s**，并新增"显式中止即强制回跳 App"。
>
> **v3.1（第一轮 v3 审查后的修正）**：
> 6. **超时预算自相矛盾**：原"单包超时 3 s × 重传 10 次 = 30 s"越过 15 s 窗口 ⇒ 改为"建立窗口 15 s + 单包超时 1.5 s + 连续 NAK ≤6 次"，主机故障后最长不可用如实报 **≤25 s**。
> 7. **强制回跳必须校验固件头**（否则会跳进被部分擦除的 App ⇒ 启动即崩 + G3 循环）。
> 8. **固件头字段来源**：静态字段（`magic/hw_id/bl_min_ver/app_ver/build_id/fmt_ver`）由 **App 源码常量**编进镜像，BL 只补写 `img_size/crc32/hdr_crc16`，**`state` 最后写**；构建断言与 CRC32 口径见 v3.2 第 16/17 条。
> 9. **WRP 结论反转**：F103xB 的 WRP 粒度为 **4 页/位**（`OB_WRP_PAGES0TO3=0x1`、`PAGES4TO7=0x2`），无法只保护 BL 而不连带 Page 6（备份页，需可写）与 Page 7（App 向量表）⇒ **本分区下 WRP 不可用**，改用代码硬边界 + 治理措施。
> 10. **备份改为全量 10 页（54~63）**：新 store 的第 1/2 次提交期间有效镜像只有 0/1 份，掉电会连 61~63 的标定一起毁掉。
> 11. **提交目标选择规则补全**（优先无效页 / u16 回绕比较 / seq 相同 tie-break），并明确"每次提交都必须从 RAM 重写 BL 槽"，否则改完波特率的后续提交会把槽抹成 `0xFF` ⇒ 失联。
> 12. **页 CRC 覆盖范围收窄**到已用区（`hdr_crc16` 覆盖 `0x000~0x009`、`data_crc16` 只覆盖 `0x010~0x10F`），避免将来启用保留区导致旧固件误判"三页全无效"而用旧备份覆盖新值。
> 13. **App IWDG 放宽到约 1 s**（提交占 40~50 ms，原 100 ms 无余量）；寿命按 50% 降额报 **15,000 次提交**。
> 14. **"可远程"限定**：XModem 不能经 Modbus 网关/PLC 转发，需透明串口隧道；升级期间要求总线静默（§6.1）。
> 15. BL 体积口径统一为 **4.7~6.0 KB（中枢 5.2）**，G1-d 有可能，并给出从便宜到昂贵的裁剪顺序。
>
> **v3.2（第二轮 v3 审查后的收口，均为一行级修订，架构不变）**：
> 16. **CRC32 改为跳过整个 32 B 固件头区**（`[base,+0x200) ∪ [base+0x220,+img_size)`），取代"按 `0xFF` 屏蔽某段"的规则 —— 三路径（主机算 `.bin` / BL 算 Flash / 启动期层 3）零歧义，且对将来头部字段增删免疫。
> 17. **构建断言修正**：`bin[0x200..0x203] == "UMFH"`（**不是 `bin[0x00..0x03]`，那里是初始 MSP**）、`bin[0x214..0x21F]` 全 `0xFF`。
> 18. **BL 擦写白名单修正为两段**：`0x08001C00~0x0800F3FF`（App 区）∪ **Page 6 `0x08001800~0x08001BFF`（仅备份未就绪时）** —— v3.1 早期只写 App 区会把备份页排除 ⇒ 备份永远建不起来 ⇒ 永久拒绝升级。App 侧守卫（`<0x0800F400` 拒绝）是另一套规则，不可混用。
> 19. **超时按波特率给公式**：单包超时 = `max(1.5 s, 2×传输时间+0.5 s)`；9600 下为 2.64 s，故主机故障后最长不可用按 **≤35 s @9600**（115200 为 ≤25 s）。
> 20. **BL 时基澄清**：推荐 `DWT->CYCCNT`（32 位 @72 MHz ≈ 59.6 s 回绕）；若用 SysTick（24 位 ≈ 0.233 s）必须累加溢出计数。
> 21. 同步残留：App IWDG 相关 4 处"100 ms"表述、`uart_config` 证据行、§5.4 增列 `iwdg.c` 改造、§12.2 门禁补 T-39/T-40、§9 增"初始化阶段提交掉电"行、§7.2 补"多无效页取物理序号最小"、§7.2 页头 CRC 括注写反修正。
> 6. 治理风险（误烧全片镜像毁掉 BL+参数区）从"列为风险"升级为**可执行的治理措施**（§10.3）。
>
> **v3.2.1（BL 开发期修订，2026-09-17）**：
> 22. **G1-d 触发与分区修订**：BL 首次全量编译实测 7,480 B（Size 优化；代码级裁剪上限约 640 B 仍超 6,144 B，且再裁将触及校验/G3/恢复等安全功能）⇒ 按本方案 §4.10 G1-d 门限执行分区重评：BL 扩为**页 0~7（8KB）**、备份页 6→**8**、App 基址 → **0x08002400（页 9~60，53,248 B，余量 7,080 B ≥4KB 门禁）**、固件头 → 0x08002600、`VECT_TAB_OFFSET=0x2400`；正文 §3/§4/§6/§9/§12/附录已同步，v3.0~v3.2 变更日志保留历史数值。依据 BL更新日志.md 决策 D7。

---

## 0. 结论速览

1. **技术可行**，且 v3 把 v2 的复杂度显著降低：BL 无需解析参数存储格式、无需日志/GC、无描述符页管理。
2. **最大的权衡不在技术而在业务**（见 §2.6）：无 A/B 双区 ⇒ 每次升级有 **8~10 s 不可用窗口**（主机故障时最长 **≤25 s @115200 / ≤35 s @9600**）、无自动回滚；且"误烧旧全片镜像一次即毁掉 BL 与参数区"是**真实的治理风险**。**若需求只是把 200+ 台现场设备升级一次，现场 SWD 升级在风险、成本、参数安全上全面优于本方案** —— 这是必须先拍板的问题（§13 Q7）。
3. 三条硬约束不变：**C1** App 必须重定位（Cortex-M3 上电复位向量固定取 `0x08000000`）；**C2** 无 A/B、无自动回滚，"可恢复"有条件；**C3** 首次切换必须 SWD。
4. **本方案最高风险项是参数区 3KB 重构与跨版本迁移**（§7），与代码现状（`eeprom.c` 无 CRC、无掉电保护、无磨损均衡）完全匹配，故排在 M1 独立完成，并把逐字段 diff（T-13）设为强制门禁。
5. BL 与 App 的唯一存储接口被压缩为 **16 B 固定槽位**（`uart_config` 1 字节 + magic + CRC16），且该接口在 M1 冻结；这消除了 v2 中"BL 依赖整卷日志格式"的耦合与格式漂移风险（用户提醒 #3）。

---

## 1. 现状实测基线

### 1.1 构建体积（`EWARM/UMF/List/UMF.map`）

```
  36'533 bytes of readonly  code memory
   9'635 bytes of readonly  data memory      <- ssd1306_fonts.o 占 6,876 B
   5'463 bytes of readwrite data memory       <- CSTACK 2,048 含在内；HEAP 未实际放置
Errors: none   Warnings: none
```

| 项 | 实测 | 说明 |
|----|------|------|
| App ROM | **46,168 B** | 最高占用末边界 `0x0800B458`（map Unused ranges 佐证） |
| App RAM | 5,463 + CSTACK 2,048 = **7,511 B** | 占 20KB 的 37%，余约 11.9KB |
| 向量表 | **236 B（0xEC）** | `.intvec` |
| 字体数据 | `0x08008D64~0x0800B307` | 并非"只在 0x08008000 以下" |

> `CLAUDE.md` 的"Flash 已用 ~34KB""IWDG 约 1 秒"均为过时/错误数据，须一并修正。

### 1.2 当前 Flash-EEPROM 页映射（逐页核对）

| 页 | 地址 | 组 | API | Len | 字节 |
|----|------|----|-----|-----|------|
| 54 | `0x0800D800` | DISPLAY `[filter_window:16\|oled_recovery:16]`、`sample_interval_ms` | 32bit | 2 | 8 |
| 55 | `0x0800DC00` | SIGNAL `[small_signal, filter_time, damping_time]` | 32bit | 3 | 12 |
| 56 | `0x0800E000` | OUTPUT `[freq_output, pulse_equiv, language]` | 32bit | 3 | 12 |
| 57 | `0x0800E400` | MEDIUM_PARAM `[density, pipe_diameter, gas_ref_press, gas_ref_temp, reynolds_k]` | 32bit | 5 | 20 |
| 58 | `0x0800E800` | SYSTEM `[modbus_addr, uart_config, total_factor, preset_total]` | 32bit | 4 | 16 |
| 59 | `0x0800EC00` | DAC 校准 `[DacZero, DacFull]` | **16bit** | 2 | 4 |
| 60 | `0x0800F000` | BASIC `[std_cond:8\|flow_unit:8\|total_unit:8]` | 32bit | 1 | 4 |
| 61 | `0x0800F400` | METER+CAL `[meter_coeff, cal_enabled, cal_k[7], cal_pct[7]]` | 32bit | 16 | 64 |
| 62 | `0x0800F800` | MEDIUM_COEFF | 32bit | 1 | 4 |
| 63 | `0x0800FC00` | SPAN `[value_4ma, value_20ma]` | 32bit | 2 | 8 |
| | | **合计** | | **37 字 + 2 半字** | **152 B** |

证据：`param_storage.c:193,203,215,228,241,257,501,535`、`main.h:93`、`bsp_menu.c:353,355,362,369,1164,1170`、`bsp_usart.c:1475,1493`、`main.c:348,363`。
`eeprom.c` 为页内追加式日志，页满条数：32bit API `240/(Len+1)`、**16bit API `480/(Len+1)`**；**无 CRC、无磨损均衡、无掉电原子性**。

### 1.3 其它关键实测事实

| 项 | 实测 | 证据 | 影响 |
|----|------|------|------|
| USART2 接收缓冲 | **150 B**（DMA 固定收 150） | `bsp_usart.h:18`、`bsp_usart.c:236,296` | v3 用 XModem 自建通道，**不受此限** |
| App FC10 长度校验 | 只比 `RxBuffer[6]==RxBuffer[5]*2`，不校验实际长度 | `bsp_usart.c:1444-1453` | 既有越界读隐患，另行修正 |
| 从站地址范围 | 1~247 | `bsp_usart.c:1785-1791` | v3 的 XModem **不再依赖地址** |
| **UART 配置为单字节** | packed：`bit[2:0]=baud, bit[4:3]=parity, bit[5]=stop` | `param_storage.h:73-81`（打包位域 + `uart_cfg_*` 内联函数）、`bsp_usart.c:1795-1803` | **BL 只需读 1 字节**（§4.7） |
| IWDG | `IWDG_PRESCALER_4`（枚举值 0x00 ⇒ 分频 4）+ 999 ⇒ **约 100 ms** | `Core/Src/iwdg.c:41-42`、`stm32f1xx_hal_iwdg.h:79` | 暖复位后仍在运行；会话期须改 5 s |
| `forward_total`/`reverse_total` | **只写 RAM，从不落盘** | `param_storage.c:620-631` | 从存储与迁移映射中剔除 |
| `pwd_operator`/`pwd_engineer` | **无 setter、无 Flash 组** | `param_storage.h`（无 setter） | 同上 |
| 旧页直写调用点 | **10 处**（见 §5.4） | 逐处核对 | 全部改造 + 写守卫 |
| 仓库无 `.ewd` | 下载/擦除策略**未纳入版本控制** | `EWARM/` 目录 | 治理风险（§10.3） |
| 陈旧副本 | `Drivers/CMSIS/Src/main.c`、`Drivers/CMSIS/Inc/main.h` 不参与编译但含旧页常量 | 实测存在 | 迁移时须排除误扫，建议删除或标注废弃 |

---

## 2. 可行性与选型

### 2.1 总量方程

`64KB = Bootloader + 旧参数备份页 + Application + 参数存储`

### 2.2 参数区为什么用 3 页（v3.1 更正原论证）

**更正**：v3.0 曾论证"参数区必须缩到 3KB 才放得下 App"——**该论证已过时**。v3 重构后参数**载荷只有 138 B、单页占用仅 272 B**，**1 页（1KB）就足以容纳**。采用 3 页是因为：

| 原因 | 说明 |
|------|------|
| **提交原子性** | 3 页轮转 ⇒ 每次提交擦"无效页或最旧页"，其余页保留旧镜像；提交中掉电零丢失。**2 页 ping-pong 也够用**，3 页额外提供"保留 2 代历史"的余量 |
| **磨损分摊** | 擦除均摊到 3 页（降额后 15,000 次提交，见 §7.2） |
| **用户已授权** | 用户的约束是"EEPROM 区可只使用 3K"，正好匹配 |

**若将来 App 空间吃紧**：可退回 **2 页（2KB）**，把 Page 61 让给 App（App 增 1 KB），代价是只保留 1 代历史 —— 这是预留的空间杠杆，不影响其它设计。

| 方案 | EEPROM | BL | App 可用 | App 余量 | 评价 |
|------|--------|----|----------|----------|------|
| 保持 10KB | 54~63 | ≤4KB | 50KB | 5,032 B | BL 仅 4KB，但独立估算需 **4.7~6.0KB** ⇒ **放不下**；且无备份页。**不可行** |
| **缩到 3KB（采用）** | 61~63 | 6,144 B + 1,024 B 备份页 = **7 KB** | **54KB** | **9,128 B** | v3 方案 |
| 依赖芯片真实 128KB | — | — | 更大 | 更大 | 部分 C8T6 实为 128KB，但**供货批次不可控**，仅作可选增强 |

**必须的前置核查**：读 `*(volatile uint16_t*)0x1FFFF7E0`（Flash 容量，单位 KB）。

### 2.3 RAM 核算

| 项 | 大小 | 说明 |
|----|------|------|
| App readwrite + CSTACK | 7,511 B | 实测 |
| 共享邮箱 | 512 B | `0x20004C00~0x20004DFF`，两工程均不链接 |
| 改动后 RAM 链接区 | 19,456 B | `0x20000000~0x20004BFF`，App 余约 11.9KB |
| BL 运行态 | ≈ 2.5 KB | XModem 1KB 包缓冲 + 128B 备份缓冲 + .bss + 1KB 栈 |

> 邮箱"天然 noinit"的依据：IAR 启动只按 INIT TABLE 初始化**链接区内**两段（`UMF.map:475-481`），邮箱在链接区之外。**注意** `CSTACK$$Limit` 是链接期符号，不能用于 `static_assert` ⇒ 用 ICF 显式 RAM 上界 + `main()` 起始处**运行期断言**。

### 2.4 三条硬约束

| 编号 | 约束 | 缓解 |
|------|------|------|
| C1 | 上电向量固定 `0x08000000`（BOOT0/BOOT1 决定别名，VTOR 不参与复位取向量）⇒ App 必须后移 | 一次性切换；保留 SWD 回滚镜像 |
| C2 | 无 A/B 双区、无自动回滚（64KB 装不下两份 46KB） | 升级窗口 **8~10 s**（主机故障 ≤25 s @115200 / ≤35 s @9600）；BL + 备份页永不擦写（由 BL 内白名单保证，**WRP 不可用**，见 §10.3）；G3 启动计数自动回退 |
| C3 | 首次切换必须 SWD | 只烧 Page 0~5，再用 RS-485 升级 App；旧参数由 BL 自动备份迁移，标定不丢 |

### 2.6 业务前提：IAP 还是 SWD（**开工前必须先拍板**）

| 判据 | 倾向 SWD | 倾向本方案（IAP） |
|------|----------|-------------------|
| 设备可触达（能开壳接线） | ✓ | |
| 预计升级频次 | 一次性 | ≥2 次/年 |
| 单次上门/停机成本 | 低 | 高 |
| 需要现场人员自助升级 | 否 | 是 |
| 设备数量与分散度 | 集中、可批量 | 分散、差旅主导 |

| 方案 | 一次性成本 | 现场成本 | 固件风险 | 参数安全 | 可远程 |
|------|-----------|----------|----------|----------|--------|
| **本方案（自研 IAP）** | 24.5~25.5 人天 + 双工程长期维护 | 一根 RS-485 | 中高（无 A/B） | 自动迁移，有备份页 | **有条件**：见下方注 |
| ST 出厂 bootloader | ≈0 | 须断开 UFL-1A 借用 PA9/PA10 | 低 | 不动 App 区 | 否 |
| **现场 SWD** | ≈0（ST-Link <¥50） | 开壳接线，约 30 台/人天 ⇒ 200 台约 7 人天 | **最低**（零固件改动） | **零丢失** | 否 |
| 换双区芯片 | 改板 + EMC 重测 | 换板 | 低（原生 A/B） | 无迁移 | 是 |
| 不做 IAP | 0 | 同 SWD | 最低 | 零丢失 | 否 |

> **"可远程"的重要限定（v3.1 新增）**：XModem 是**裸串口点对点协议**，**不能经 Modbus 网关/PLC/DTU 的寄存器通道转发**。因此"远程升级"只在两种部署下成立：① 现场有**透明串口隧道**（TCP Server / 串口服务器，把 RS-485 直通到上位机）；② 有人到现场用笔记本直连。若现有架构是"Modbus 网关汇聚"，则**远程升级不成立**，只能现场直连升级 —— 这会显著削弱本方案相对 SWD 的收益，请在 §13 Q7 一并确认。

**判据结论**：若 (i) 设备可触达 **且** (ii) 只需一次性升级 ⇒ **选现场 SWD，本方案暂缓**（可省 24.5~25.5 人天与全部 C1/C2/C3 风险）。
只有出现"设备不可达 / 年升级 ≥2 次 / 需现场自助"之一时，本方案的投入才回本；且"远程"成立的前提是存在**透明串口隧道**（见上表注）。**该结论写入 §13 Q7，请用户拍板。**

---

## 3. 终版 Flash / RAM 分区

| 区域 | 页 | 地址 | 大小 | 写入方 |
|------|----|------|------|--------|
| Bootloader | 0~7 | `0x08000000~0x08001FFF` | 8,192 B | 仅 SWD |
| 旧参数备份页 | 8 | `0x08002000~0x080023FF` | 1,024 B | 仅 BL，首次会话写入后不再擦 |
| **Application** | **9~60** | **`0x08002400~0x0800F3FF`** | **53,248 B** | BL 升级写入 |
| 参数存储（3 页轮转） | 61~63 | `0x0800F400~0x0800FFFF` | 3,072 B | App |

合计 8,192 + 1,024 + 53,248 + 3,072 = **65,536 B = 64KB** ✓；App 余量 **7,080 B（≥4KB 门禁满足）**。
App 基址 `0x08002400`（页对齐，0x200 倍数满足 VTOR 对齐）；`VECT_TAB_OFFSET = 0x2400`；**固件头位于 `0x08002600`（App+0x200，与向量表同页）**。

> **G1-d 分区修订（开发期 v3.2.1，2026-09-17）**：v3.2 原 6KB BL 分区（页 0~5 BL / 页 6 备份 /
> 页 7~60 App 55,296 B 余量 9,128 B）在 BL 首次全量编译实测 **7,480 B（Size 优化 + 已按
> §4.2 裁剪顺序评估：代码级裁剪上限 ~640B 仍超限，且再裁将触及安全功能）**，触发 §4.10
> G1-d 门限（"裁剪后仍 >6.0KB ⇒ 方案级重评"）。修订：BL 扩为页 0~7（8KB，实测余量 712 B），
> 备份页 6→8，App 基址 0x08001C00→0x08002400（52KB，余量 7,080 B 仍满足"≥4KB"门禁）。
> 其余设计（固件头偏移 +0x200、参数页 61~63、RAM 布局、白名单两段式）不变。
> 本文以下章节的地址/长度常量已按本修订更新；v3.0/v3.1/v3.2 变更日志中的历史数值保留原样。

ICF 变更：
```c
/* App: EWARM/stm32f103xb_flash.icf */
define symbol __ICFEDIT_intvec_start__     = 0x08002400;
define symbol __ICFEDIT_region_ROM_start__ = 0x08002400;
define symbol __ICFEDIT_region_ROM_end__   = 0x0800F3FF;   /* Page 9~60 */
define symbol __ICFEDIT_region_RAM_end__   = 0x20004BFF;   /* 让出邮箱区 */
place at address mem:0x08002600 { readonly section .fw_header };
```
```c
/* BL: EWARM/UMF_Boot.icf —— ROM 0x08000000~0x08001FFF（页 8 及之后不在 BL 链接区内），RAM 同 App */
```
> **v2 单页布局越界教训**（保留为设计红线）：1KB 页合法偏移仅 `0x000~0x3FF`；v2 曾把备份块放在 `0x400`，而 `0x400` 即页容量 ⇒ 落到 Page 8 的 App 向量表上。**任何页内偏移都必须显式断言。**

---

## 4. Bootloader 设计

### 4.1 技术选型

| 决策 | 选择 | 理由 |
|------|------|------|
| 实现层次 | **寄存器级裸机，零 HAL** | 实测 `UMF.map` 显示 ILINK 有函数级死代码消除（同 `.o` 的阻塞式 `HAL_UART_Transmit/Receive` 未链接），但 HAL 子集仍需 **6.5~8.0KB** ⇒ 6,144 B 区**放不下**；寄存器级独立估算 **4.7~6.0 KB（中枢 ≈5.2）** ⇒ 落在 G1-b/c，**G1-d 有可能**（见 §4.2） |
| 传输 | **USART2 / RS-485，XModem-CRC（1K 与 128B 包都收）** | RS-485 是客户现成在用的对外总线（确定可达）；XModem 点对点**无需从站地址**，取消 v2 的地址依赖链；`USART1/PA9-PA10` 是否引出未确认且与 UFL-1A 模组 TX 争用（见 §13 Q10） |
| 波特率 | 由参数区固定槽位给出（§4.7），缺省 115200 8N1 | `uart_config` 仅 1 字节 |
| 防砖 | **固件头内嵌 App、最后写**（§4.5） | 不占额外页；头区随 App 区被擦除 ⇒ 天然无陈旧 VALID |
| BL 反馈 | PB5 电源 LED（快闪=升级中，慢闪=等待，长亮=完成） | 零成本，无 OLED |
| 看门狗 | 入口立即喂狗；会话期改 **5 s**；页擦/写前后喂狗 | IWDG 实测约 100ms 且暖复位后仍在运行（§4.6） |
| 编译约束 | **零 float**（软浮点库会 +2~3KB 击穿预算） | 版本号/CRC/时序全整数 |

### 4.2 代码预算与门限

| 模块 | 预算 |
|------|------|
| 向量表 + 启动 + 时钟（HSE 8M×9=72MHz，失败回退 HSI） | 1.0~1.2 KB |
| Flash 驱动（解锁/页擦/半字编程/等待/错误） | 0.5 KB |
| USART2 驱动 + PA1 DE 半双工时序 | 0.6 KB |
| XModem-CRC 状态机（SOH/STX/EOT/CAN、包号、重传、超时） | 0.9 KB |
| CRC32（按位）+ CRC16-CCITT（按位） | 0.3 KB |
| 旧参数抽取器（`legacy_param_read`，与 App 共享） | 0.4 KB |
| 参数页读取（选页 = 只读 3 个 16 B 页头的 magic/state/seq + 槽自身 CRC，**不算页数据 CRC**） | 0.2 KB |
| 跳转/校验/模式判定/LED | 0.5 KB |
| **合计** | **≈ 4.7~6.0 KB（中枢 ≈5.2；上限 6,144 B）——G1-d 前预算口径，实测见 §3 修订块** |

| 门限 | 判据 | 动作 |
|------|------|------|
| G1-a | ≤ 4.5 KB | 通过，余量充足 |
| G1-b | 4.5 ~ 5.2 KB | 通过（预期中枢），需为后续修补预留 |
| G1-c | 5.2 ~ 6.0 KB | 通过但需**按下列顺序裁剪** |
| G1-d | 裁剪后仍 > 6.0 KB | **方案级重评**（见下） |

**裁剪顺序（从最便宜开始，R1 建议）**：① 去掉 128 B 双包型（仅保留 1K 包 —— 发送端由我们的工具控制，双包型只是兼容性冗余）；② 精简 LED 花式指示；③ 合并 CRC32/CRC16 的循环体；④ 精简错误分支的日志字符串。**"让出备份页给 BL"是最贵的一刀**（首次迁移退化为依赖上位机手工备份，见方案 B 的操作风险），必须排在最后，且需重新评审。

> 体积必须**在 M2 用一次真实编译钉死**（编译寄存器级骨架 + 全部模块即可粗判），不得拖到 M3。

### 4.3 启动流程

```
复位
 ├─ 立即喂狗一次（暖复位后 IWDG 仍带残余计数：当前固件约 100 ms，v3.1 放宽后约 1 s）
 │   ★ 喂狗必须先于时钟初始化：HSE 失败路径的有界等待最长 ~150 ms（100k 次循环），
 │     旧固件 100 ms 预算下先喂狗才能避免复位循环（BL 开发 S4 审查修正）
 ├─ 时钟初始化（HSE 失败 → HSI 8MHz 降级，仍可升级）
 ├─ 读通信参数（§4.7）→ 配置 USART2 + PA1
 ├─ 读固件头（0x08002600）
 ├─ 若【需要备份旧参数】→ 抽取 Page 54~60 → 写 Page 8（副本）+ 回读 + CRC + 字段区间校验
 │     备份未就绪 ⇒ **拒绝一切擦除类命令**（不破坏旧 App）
 ├─ 模式判定：
 │     邮箱 cmd == CMD_UPGRADE            → 升级模式
 │     固件头无效（App 不可信）           → 升级模式
 │     PA0 长按 ≥3 s 且二次确认           → 升级模式（物理恢复）
 │     否则                                → 跳转 App（冷启动 <150 ms、暖复位 <100 ms）
 └─ 升级模式：15 s 窗口内每 1 s 发 'C'
      收到数据 → XModem 状态机 → EOT → 全镜像 CRC32（**跳过 32 B 固件头区**）→ 写固件头 → 跳转
      窗口耗尽且 App 有效 → 跳转 App；App 无效 → 继续等待（LED 慢闪）
```

### 4.4 跳转（顺序不可颠倒）

```c
__disable_irq();
SysTick->CTRL = 0; SysTick->LOAD = 0; SysTick->VAL = 0;
for (i = 0; i < 8; i++) { NVIC->ICER[i] = 0xFFFFFFFF; NVIC->ICPR[i] = 0xFFFFFFFF; }
/* ★ ICER/ICPR 覆盖不到 SysTick(异常15)/PendSV —— 它们在 SCB->ICSR。若不先清挂起位，
 *   下面的 __enable_irq() 会立刻取向量到"App 的"处理函数，而 App 尚未初始化。 */
SCB->ICSR = SCB_ICSR_PENDSTCLR_Msk | SCB_ICSR_PENDSVCLR_Msk;
/* 向量表校验：SP ∈ [0x20000000,0x20004BFF] 且 8 字节对齐；
 *             PC ∈ [0x08002400,0x0800F3FF] 且 bit0==1；两字均非 0xFFFFFFFF/0x00000000 */
bl_iwdg_set_period(5000); bl_iwdg_feed();    /* 覆盖 App 启动期（其 MX_IWDG_Init 在 main.c:147） */
bl_clock_deinit();                            /* 回到 HSI 8MHz，避免 App 时钟初始化异常 */
SCB->VTOR = APP_BASE; __DSB(); __ISB();       /* 先改 VTOR，消除挂起中断窗口 */
__set_MSP(*(volatile uint32_t*)APP_BASE);
__enable_irq();                               /* ★ 必须！PRIMASK 不随跳转清零 */
__DSB(); __ISB();
((void (*)(void))(*(volatile uint32_t*)(APP_BASE + 4)))();
```
**为什么 `__enable_irq()` 不能省**（v3 保留 v2 的这条教训，也是方案 B 的致命缺陷，见对比报告）：PRIMASK 是内核寄存器，跳转不影响它；而 App 自身**从不使能中断**（`Core/Src/main.c` 全文无 `__enable_irq`，`:380` 的 `__disable_irq()` 在 `Error_Handler` 内；`HAL_Init` 不做；`OLED/ssd1306.c:69-75,82-90` 只是"保存-恢复"惯用法，PRIMASK=1 时不会打开；IAR 启动文件无 `CPSIE`）。App 启动期即调用依赖 SysTick 的 `HAL_Delay`（`ssd1306.c:60,62,103,105,236`）⇒ 遗漏将导致**卡死 + 100ms 看门狗复位循环 + 看起来像砖**。

### 4.5 固件头（防砖核心）

位于 **`App + 0x200` = `0x08002600`**（页 9 内，与向量表 236 B 不重叠），由 App 链接脚本以 `.fw_header` 保留 **32 B**。

**字段来源必须两类分开（v3.1 修正 —— 原 v3 未定义来源，会让 `hw_id`/`bl_min_ver` 校验空转）**：

| 偏移 | 字段 | 类型 | 来源 | 说明 |
|------|------|------|------|------|
| 0x00 | `magic` | u32 | **App 静态** | `0x554D4648`("UMFH")，编进 `.fw_header` |
| 0x04 | `hw_id` | u16 | **App 静态** | `0x0103` = STM32F103C8 |
| 0x06 | `bl_min_ver` | u16 | **App 静态** | 该 App 要求的 BL 最低版本 |
| 0x08 | `app_ver` | u32 | **App 静态** | `0x00MMmmpp`（App 源码 `APP_VERSION` 宏） |
| 0x0C | `build_id` | u32 | **App 静态** | 构建序号，决定层 3 是否跳过 |
| 0x10 | `fmt_ver` | u8 | **App 静态** | 头部格式版本 = 1 |
| 0x11 | `rsvd0` | u8×3 | **App 静态** | `0xFF` |
| — | **↓ BL 写入区（镜像中恒为 `0xFF`，共 11 B，连续）↓** | | | |
| 0x14 | `img_size` | u32 | **BL 写** | App 镜像字节数，偶数，`0x220 ≤ x ≤ 53,248` |
| 0x18 | `crc32` | u32 | **BL 写** | App 镜像 CRC-32/ISO-HDLC（口径见规则 3） |
| 0x1C | `hdr_crc16` | u16 | **BL 写** | 覆盖 `0x00~0x1B`（全部静态字段 + `img_size` + `crc32`；**不含自身与 `state`**） |
| 0x1E | **`state`** | u8 | **BL 最后写** | `0xA5` = 有效。**唯一有效性标记，最后一步写入** |
| 0x1F | `rsvd1` | u8 | App 静态 | `0xFF` |

> **为什么把静态字段与 BL 写入区分成两块连续区（v3.1 修正的关键）**：v3.1 早期写"头区 32 B 全部按 `0xFF` 计入 CRC32"，这在"静态字段已编进镜像"之后是**错的** —— `magic`/`app_ver` 等静态字段在 Flash 里是真实值。**正确做法是 CRC32 直接跳过整个 32 B 头区**（见规则 3）；而 `0x14~0x1F`（BL 写入区 + `rsvd1`，共 12 B）是**构建断言**要检查的范围，且该区必须连续，便于实现与断言。

**实现方式（无 merge 脚本、无主机元数据交换）**：App 源码里定义
```c
/* BSP/boot_flag.c —— 必须是"实例化的 const 对象"，不能是链接器空洞 */
#pragma location = ".fw_header"
__root const uint8_t fw_header[32] = {
    /*0x00*/ 0x48,0x46,0x4D,0x55,  /* "UMFH" */
    /*0x04*/ 0x03,0x01, 0x01,0x00,  /* hw_id, bl_min_ver */
    /*0x08*/ <APP_VERSION 4B>,      /* app_ver  */
    /*0x0C*/ <BUILD_ID 4B>,         /* build_id */
    /*0x10*/ 0x01,0xFF,0xFF,0xFF,   /* fmt_ver, rsvd0 */
    /*0x14*/ 0xFF,0xFF,0xFF,0xFF,   /* img_size  ← BL 写 */
    /*0x18*/ 0xFF,0xFF,0xFF,0xFF,   /* crc32     ← BL 写 */
    /*0x1C*/ 0xFF,0xFF,             /* hdr_crc16 ← BL 写 */
    /*0x1E*/ 0xFF,                  /* state     ← BL 最后写 */
    /*0x1F*/ 0xFF
};
```
⇒ 静态字段随镜像一起被写入；BL 只需在 EOT 之后**按 1→0 编程** `img_size`/`crc32`/`hdr_crc16`/`state` 这 4 处（此刻仍是 `0xFFFF`，属合法编程）。

**四条硬性规则**：
1. **`state` 最后写**：升级中途断电 ⇒ `state` 仍为 `0xFF` ⇒ BL 判无效 ⇒ 停留升级模式 ⇒ 可重新升级。**`state=0xA5` 存在 ⇒ 全部数据页与头部其余字段均已写入**（前提：BL 严格在最后一个数据包 ACK 之后才写，且每个半字编程都检查状态）。
2. **写头之前禁止再擦页 7**（页 7 含向量表；头写入是"补写 4 处仍为 `0xFF` 的字"，**不需要也不允许再次擦页**）。
3. **CRC32 口径统一为"跳过整个 32 B 固件头区"**（采用 R1 建议：零歧义，且对将来头部新增/移动字段免疫）：
   覆盖范围 = **`[APP_BASE, APP_BASE+0x200)` ∪ `[APP_BASE+0x220, APP_BASE+img_size)`**。三条路径完全一致：
   ① 主机对 `.bin` 计算时跳过 `0x200~0x21F`；② BL 对 Flash 计算时跳过同一区间（**与头区当前是 `0xFF` 还是真实静态值无关**）；③ 启动期层 3 复算同样跳过。
   **不设置任何"按 `0xFF` 屏蔽"的规则** —— v3.1 早期版本的缺陷正是源于"静态字段已编进镜像"与"头区整体屏蔽"这两条互斥规则并存。
   App 区末尾未用到的页（`0xFF`）不计入。
4. **构建产物必须校验**（否则会出现"升级成功但永不启动"）：
   - `.fw_header` 必须是**实例化的 32 B const 对象**。若只是链接器空洞，`ielftool` 会以 **`0x00`** 填洞 ⇒ 静态字段不对 ⇒ 层 1 直接判无效。
   - 后构建断言（任一不满足即**构建失败**）：
     ① `.bin` 长度 `≥ 0x220` 且为偶数；
     ② **`bin[0x200..0x203] == "UMFH"`**（固件头确实在 `App+0x200 = 镜像偏移 0x200`）；
     ③ **`bin[0x214..0x21F]` 全为 `0xFF`**（= 固件头偏移 `0x14~0x1F`，即 BL 写入的 `img_size`/`crc32`/`hdr_crc16`/`state` 四处 + `rsvd1`，占位必须正确）；
     ④ `word0`（初始 MSP）∈ RAM 区、`word1`（Reset_Handler）∈ `0x08002400~0x0800F3FF` 且 bit0=1。
     > ⚠ 断言 ② 的偏移是 **`0x200`**，不是 `0x00` —— `bin[0x00..0x03]` 是 App 的初始 MSP 值（`0x2000xxxx`）。v3.1 早期把这两处写混，会让正确构建被误判失败。

**启动期校验分层**（避免热路径超时）：
   | 层 | 内容 | 何时 |
   |----|------|------|
   | 1 | **按序**：头区在镜像范围内 → `magic` → `hw_id` → `bl_min_ver` → 字段合法性（`0 < img_size ≤ 53,248` 偶数、`crc32 != 0xFFFFFFFF`）→ **`hdr_crc16`（最后，用于拦下半写的头）** → `state == 0xA5` | 每次上电 |
   | 2 | 向量表合法性（SP/PC 范围与 Thumb 位，且两字非全 `0xFFFFFFFF`/`0x00000000`） | 每次上电 |
   | 3 | 整镜像 CRC32 复算（**跳过 32 B 头区** `0x200~0x21F`；按位约 40~70 ms，T-34 实测钉死） | **冷启动必做**；暖复位且头 `build_id`/`state` 未变 ⇒ 跳过 |
   > **口径统一（v3.1）**：层 3 的"何时"以本表为准（§4.3 流程图中的"冷启动 <150 ms"即含层 3）。
   > 有"state 最后写"后，升级致损路径已由第 1 层覆盖；第 3 层的增量是域外损坏（位翻转、误用 SWD）。冷启动多花约 50 ms 是可接受的保险，故默认保留。

### 4.6 XModem-CRC 时序与看门狗

- 包格式：`SOH(0x01)+128B` 或 `STX(0x02)+1024B`，包号 + 反码包号 + 数据 + CRC16；`EOT(0x04)` 结束；`CAN(0x18)`×2 中止。**1K 包正好等于 1 个 Flash 页**（App 基址页对齐）。
- **包数**：App 46,168 B ⇒ `45 × 1024 + 88` ⇒ **46 个包**（前 45 个满 1K，**末包仅 88 B 有效、按 XModem 规范填充到 1K**），覆盖 **46 页**擦写；上限 53,248 B = 52 个满 1K 包。
- **⚠ 升级期间必须总线静默（v3.1 新增）**：XModem **没有任何地址过滤**，BL 在等待数据时把任意以 `0x01`/`0x02` 开头的字节流当作包头 —— 同总线其它从站的应答帧或网关广播都可能命中，造成反复 NAK/重传。
  - 操作前提：升级时**暂停其它 Modbus 轮询 / 隔离目标设备**，最好点对点直连（USB-RS485 转换器）。
  - 后果界定：由于每包都有 CRC16 + 包号校验，误判**只会导致 NAK 与重传，不会写错数据**；但若总线持续繁忙，会话可能反复中止（⇒ 不可用窗口拉长到 §4.6 的上限）。
  - 与 §2.6 的"可远程"限定一致：**不能经 Modbus 网关/PLC 转发**，需透明串口隧道。
- **ACK 时序硬约束**：必须先收满整包 → 校验 CRC16 → 擦/写 → **编程完成后才发 ACK**。发送方在等 ACK 期间不发数据，故 F1 页擦（≤40 ms）与页编程（1 KB ≈ 15 ms）造成的停顿不会丢字节。
- 半双工 RS-485：收包期间 PA1=低（接收），发 ACK 前拉高，发完 `TC=1` 后拉低并保持 ≥1 字符时间。
- **超时/重传与会话窗口（v3.1 修正 —— 原表述自相矛盾）**：

  | 阶段 | 参数 | 说明 |
  |------|------|------|
  | 会话建立窗口（尚未收到任何包） | **15 s** | BL 每 1 s 发 'C'；到期且 App 有效 ⇒ 跳回 App |
  | 传输中单包超时 | **`max(1.5 s, 2×单包传输时间 + 0.5 s)`**：115200 ⇒ 1.5 s；**9600 ⇒ 2.64 s**（1K 包传输 1.07 s） | 收到任意有效字节即重置计时 |
  | 连续 NAK 上限 | **6 次**：115200 ⇒ ≈9 s；9600 ⇒ ≈15.8 s | 超过 ⇒ 中止会话 |
  | 中止后 | 回到会话建立窗口（再 15 s） | App 有效则 15 s 后跳回 |
  | **主机故障后最长不可用** | 115200 ⇒ **≈24 s（≤25 s）**；**9600 ⇒ ≈30.8 s（≤35 s）** | 原 v3 写"≤15 s"是**错的**：单包超时 3 s × 重传 10 次 = 30 s 本就会越过 15 s 窗口 |

  - 任意有效字节/包到达 ⇒ **重置**会话空闲计时（避免"主机仍在重传但 BL 已超时跳回"）。
  - 工具主动"取消"不受该窗口约束（见 §8），但**必须先通过固件头与向量表校验**才能立即回跳。
- **看门狗**：会话期 IWDG 改 **5 s**（`IWDG_PRESCALER_64`（枚举值 0x04 ⇒ 分频 64）+ Reload 3124 ⇒ 64×3125/40 kHz ≈ 5.0 s）；改 `PR`/`RLR` 后**必须立即写 `KR=0xAAAA` 重载**并等 `PVU/LVU` 清零；喂狗点 = 每收完一包 + **在"等待字节"的轮询循环内每约 100 ms 一次**（不能只在包间喂 —— 单包超时期间若只靠包间喂狗会超时）+ 每次页擦/写前后。
- **BL 时基（v3.1 更正）**：推荐 **`DWT->CYCCNT`**（32 位 @72 MHz ⇒ 约 **59.6 s** 回绕，远大于 5 s 看门狗与最大超时，且无需中断）；若用 **SysTick**（24 位 @72 MHz ⇒ 约 **0.233 s** 回绕）则**必须累加溢出计数**。两者均**不依赖 HAL**。（v3.1 早期把两者混写成"计 1 s 约 0.23 s 溢出量"，是错的。）
- **App 侧配套**：① `main()` **最开头**加一次原始喂狗 `IWDG->KR = 0xAAAA;`（未启动时写入无害）；② **把 App 的 IWDG 从约 100 ms 放宽到约 1 s**（`IWDG_PRESCALER_64` + Reload 624 ⇒ 64×625/40 kHz = 1.0 s），既给参数提交（40~50 ms）留出余量，也消除"暖复位后 100 ms 看门狗"的复位循环隐患。

### 4.7 BL 的通信参数来源（用户提醒 #3 的直接回应）

**v2 的做法**：BL 内置约 400 B 的"新存储日志扫描器"作为权威来源 ⇒ 位于"失联即变砖"链条上，且要求整卷日志格式冻结，预算与纪律都不宽松。

**v3 改为固定槽位**（BL 只需 **1 字节**）：

| 优先级 | 来源 | 代价 | 适用 |
|--------|------|------|------|
| 1 | RAM 邮箱（magic + 反码 + CRC32 校验通过） | ~20 B | App 主动请求升级（暖复位） |
| 2 | **参数页固定槽位 `+0x100`**：`{magic u32="BLCF", uart_config u8, rsvd[3], slot_crc16 u16, rsvd[6]}`，从 Page 61/62/63 中取"页头 `magic`+`state` 有效且 `seq` 最大"的那一页读取（**不计算页数据 CRC**，故 BL 侧仅约 0.2 KB） | 0.2 KB（含选页） | 冷启动（权威来源） |
| 3 | 缺省 `115200 8N1`（baud_idx=4） | 0 | 全新设备 / 槽位不可用 |
| 4 | **工具侧波特率/校验/停止位扫描**（不是"指定"—— 工具无法强制 BL 换档，只能扫描） | 0（工具侧） | 运维手动兜底：按 `115200→9600→19200→38400→4800→2400` × `{8N1, 8E1, 8O1}` 共 18 组、每组 1.5 s（**总计 ≤27 s**），监听 'C' |
| 5 | **物理恢复模式**：PA0 长按 ≥3 s **且**再按一次确认键 ⇒ 固定 115200，仅监听 | ~40 B | 最后手段 |

> **槽内 `uart_config` 必须做语义校验**（否则异常字节会配出非法波特率）：`baud_idx ≤ 5`（`BAUD_RATE_COUNT`）、`parity ≤ 2`、`stop ≤ 1`，越界即视为槽无效并回落优先级 3。
>
> **App 侧写入顺序（v3.1 新增，闭合"改完波特率后失联"的时序窗口）**：App 在**把新的 `uart_config` 应用到自身 UART 之前**，必须先提交一次含新槽值的参数镜像并**回读校验**；校验通过后才切换自身波特率。否则"已按新波特率通信、但槽位仍是旧值"的窗口内一旦掉电，冷启动 BL 会取到旧波特率而失联（配 T-15 扩展用例）。

> **为什么这基本切断了"失联即变砖"**：XModem 是点对点协议，BL **不需要从站地址**；唯一需要的配置是 1 字节 `uart_config`（打包语义见 `param_storage.h:73-81` 与 `bsp_usart.c:1795`：`bit[2:0]=baud, bit[4:3]=parity, bit[5]=stop`）。槽位随参数页一起提交（§7.2），格式是 16 B 定长结构而非日志 ⇒ BL 与 App 之间只需冻结这 16 B，**不再依赖存储格式**。
> **残留失联点（必须如实说明）**：若三页参数全损坏 **且** 用户把 uart_config 改成了非缺省值（例如 9600 或 8E1），则优先级 2/3 都失效，BL 只能用缺省的 **115200 8N1** —— 而设备实际工作在别的档位 ⇒ 工具必须靠**优先级 4 的 18 组扫描**才能连上。这是本链唯一残留的失联点，兜底是优先级 5（PA0 恢复模式 + 115200 8N1）。T-16 / T-25 覆盖此路径。
> **PA0 就是面板确认键（`main.h:69-70`）且兼作 WKUP**，"上电时按住确认键"是自然动作 ⇒ 必须"≥3 s + 二次确认"，并配 T-36。

---

## 5. Application 改造

### 5.1 链接与中断向量表

1. `EWARM/stm32f103xb_flash.icf` 按 §3 修改 + 新增 `.fw_header` 保留区；
2. `Core/Src/system_stm32f1xx.c`：**启用 `USER_VECT_TAB_ADDRESS`**（当前被注释 ⇒ `SCB->VTOR` 从未设置，迁移后不设置则所有中断按 `0x08000000` 取向量并跑飞）：
```c
#define USER_VECT_TAB_ADDRESS
#define VECT_TAB_OFFSET  0x00002400U   /* G1-d 修订（原 0x1C00）*/
```
   注意 `Core/Src/system_stm32f1xx.c` 与 `Drivers/CMSIS/Src/system_stm32f1xx.c` 同名，只改**实际参与编译**的那个（`UMF.ewp` 只列 `Core/Src`）。
3. `BSP/boot_flag.c/h`（新增）：`.fw_header` 保留区定义 + RAM 魔数读写 + 运行期 `CSTACK$$Limit` 断言。

### 5.2 共享邮箱（`0x20004C00`，512 B）

| 偏移 | 字段 | 说明 |
|------|------|------|
| 0x00 | `magic` = `0x424C4F54`("BLOT") | 写入方最后写 |
| 0x04 | `magic_inv` | 反码 |
| 0x08 | `seq` | 每次写递增 |
| 0x0C | `cmd` | 0=无 / 1=请求升级 |
| 0x10 | `uart_config`（低 8 位） | 与 `param_storage.h:77-81` 语义一致 |
| 0x14 | `boot_attempt` | **G3 启动计数**：BL 跳转前 +1、App 健康后清零；**仅当 magic+反码+CRC32 全通过才采信**，否则视为 0 |
| 0x18 | `last_verified_build_id` | **D3（开发期冻结）**：BL 层 3 复算通过后写入的 build_id；暖复位且与当前固件头 build_id 相同 ⇒ 跳过层 3 |
| 0x1C | `crc32` | 覆盖 `0x00~0x1B`（28 B） |
| 0x20~0x1FF | 保留 | |

全部字段以 `volatile` 访问；读取方校验 magic + 反码 + CRC32 三者。邮箱在**冷启动后内容无定义**，故只作暖复位快速通道（§4.7 优先级 1）。

### 5.3 App 侧新增 Modbus 寄存器

| 寄存器 | PDU | 名称 | 读写 | 说明 |
|--------|-----|------|------|------|
| 40127 | 126 | IAP_BOOT_REQUEST | FC06 | 写 `0x5AA5` → 回响应后置邮箱 + `NVIC_SystemReset()` 进 BL |
| 40128~40129 | 127~128 | APP_VERSION | FC03 | u32，取自固件头 `app_ver` |
| 40130 | 129 | IAP_FLAG_MIRROR | FC03 | 回显 |
| 40131 | 130 | PARAM_STATUS | FC03 | bit0=已迁移 / bit1=部分迁移 / bit2=含默认值 |

现有 App 最大寄存器为 `ExtParamEndAddr=125`（`bsp_usart.h:135`），无冲突。**寄存器编号统一用 4x 前缀并标注 PDU 0 基地址**，避免 off-by-one。

### 5.4 启动顺序与必须改造的调用点

```
boot_flag_ram_assert();        /* 运行期断言 RAM 上界 */
boot_mailbox_read();           /* 读邮箱，cmd 暂存 RAM；迁移确认后才清除 */
param_storage_init();          /* 内部完成：选页 → 若有旧格式则迁移 → 载入 RAM 缓存 */
Data_Init();                   /* 重写：DAC/Span 一律经 param_get_*，禁止直读旧页 */
... 其余外设初始化 ...
boot_mailbox_clear();          /* 迁移确已完成才清 cmd */
```
**三条顺序硬性要求**：① 迁移必须早于 `Data_Init()`（v1 中 `Data_Init` 在 `main.c:130` 先于 `param_storage_init` 的 `:133`，且直读 Page 63/59）；② 邮箱 `cmd` 必须在迁移确认后清除；③ `param_storage_init()` 内不得无条件落盘（现码 `param_storage.c:325` 在"METRE 页全空"分支会 `flush_meter_cal_group()` —— 改成 3 页轮转后须确保该分支只在真正未初始化时触发一次）。

**必须改造的 10 处旧页直接调用**（逐处核对）：

| 位置 | 现状 | 改造 |
|------|------|------|
| `Core/Inc/main.h:93` | `DAC_FLASH_PAGE_ADDR = ADDR_FLASH_PAGE_59` | 改为 `param_store` 键 |
| `BSP/param_storage.c:117-124` | Page 54~61 宏 | 全部改为新偏移 |
| `Core/Src/main.c:348,363` | 直读 Page 63/59 | `param_store_load_span()/load_dac()` |
| `bsp_menu.c:353,355,362,369,1164,1170` | 直写 Page 59/63 | `param_store_save_dac()/save_span()` |
| `bsp_usart.c:1475,1493` | 直写 Page 59/63 | 同上 |

并**在存储层加防御性守卫：目标地址 < `0x0800F400` 的擦/写请求一律拒绝**（`icf:12-16` 已记录过同类"常量被参数写入擦除"的自毁 HardFault）。配 T-35。
> **另需一并改造**：`Core/Src/iwdg.c:41-42` 的 App IWDG 由 `IWDG_PRESCALER_4`+999（约 100 ms）改为 **`IWDG_PRESCALER_64`+624（约 1 s）**，并在 `main()` 最开头加原始喂狗（理由见 §4.6/§7.2）。

### 5.5 工程与下载治理（工程侧必做）

| 项 | 现状 | 动作 |
|----|------|------|
| IAR 工作区 | `Project.eww` 仅含 `UMF.ewp` | 手工加入 `UMF_Boot.ewp`；两工程 `Obj/`、`List/` 分开 |
| `.ewp` 源文件 | IAR 不自动发现新文件 | `Boot/*.c` 与新增 App 源文件均须手工加 `<group>` |
| Post-build | **当前无任何 Post-build** | 新增 `ielftool --bin` 生成 App 裸镜像；并校验其长度/向量表 |
| **下载配置** | **仓库没有 `.ewd`** ⇒ 擦除策略不受版本控制 | 固化"仅按段擦除"配置并**把 `.ewd` 纳入版本控制**；README 明令禁止整片擦除 |
| ICF 串用 | `IlinkIcfOverride=1`（`ewp:781-786`） | 两工程各自固定 ICF 路径 |
| 选项字节 | `main.c:309` 的 `FlashProtect()` 是**死代码** | **禁止任何 OB/RDP 写入**（RDP 变更会触发 mass erase，一次抹掉 BL 与参数区） |
| 断言 | 无 | `sizeof`/`offsetof` 用 `_Static_assert`；RAM 上界用 ICF + 运行期断言 |

---

## 6. 升级协议（XModem-CRC over RS-485）

### 6.1 会话时序

```
主机(工具)                                   设备(BL)
   │  ← 'C'(0x43) 每 1s 一次，共 15s ────────│ 进入升级模式
   │──── SOH + 128B 包 (或 STX + 1024B 包) ──→│ CRC16 校验 + 包号校验
   │                                          │ 首次触及该页 ⇒ 擦页
   │                                          │ 半字编程（逐个检查状态）
   │  ←──────────── ACK(0x06) ────────────────│ 编程完成后才回 ACK
   │            ... 重复 46 个 1K 包 ...        │
   │──── EOT(0x04) ──────────────────────────→│ 全镜像 CRC32（跳过 32 B 固件头区）
   │                                          │ 通过 ⇒ 写固件头（最后一步）
   │  ←──────────── ACK ──────────────────────│ LED 长亮
   │                                          │ 跳转 App
```
- 全流程约 **6~8 s**（46 个 1K 包 @115200）；含握手与校验 **约 8~10 s**；9600 时约 55~60 s。
- 中止：`CAN`×2、重传耗尽、或主机断开 ⇒ 回会话建立窗口；窗口内无数据且 App 有效 ⇒ 跳转 App（**跳转前一律做固件头 + 向量表校验**）。
- 目标机 **任何时刻失败都不影响 BL**（BL 与备份页从不参与擦写）。

### 6.2 镜像与校验

| 项 | 规则 |
|----|------|
| 传输内容 | **纯 App 镜像**（`0x08002400` 起），不含 BL |
| 主机预检 | `word0 ∈ [0x20000000,0x20004BFF]`、`word1 ∈ [0x08002400,0x0800F3FF]` 且 bit0=1、两字均非 `0xFFFFFFFF`/`0x00000000`、长度偶数且 **`0x220 ≤ len ≤ 53,248`**（下界取 `0x220` 而非向量表长度 `0xEC`：**必须保证头区 `0x200~0x21F` 落在镜像内**，否则 BL 写头会写到镜像之外）、CRCs 自算 |
| **CRC 覆盖口径（统一）** | 整镜像 CRC32 **跳过 32 B 固件头区**：`[APP_BASE, +0x200) ∪ [APP_BASE+0x220, +img_size)`。三路径一致（主机算 `.bin` / BL 算 Flash / 启动期层 3 复算），**均与本区当前内容无关**，故不存在"头区该按 0xFF 还是真实值"的歧义。任一环节改动该口径都会导致"升级稳定失败在最后一步" |
| **镜像填充与断言（v3.1）** | 后构建必须生成**含头区占位**的完整镜像：`ielftool --bin --fill 0xFF $TARGET_PATH$ UMF_app.bin`；随后断言：① `len ≥ 0x220` 且偶数；② `bin[0x200..0x203] == "UMFH"`（**不是 `bin[0x00..0x03]`，那里是初始 MSP**）；③ `bin[0x214..0x21F]` 全为 `0xFF`；④ `word0/word1` 合法。**不满足即构建失败**（配 T-40） |
| 元数据 | 与 `.bin` 同目录的 `UMF_app.json`：`app_ver`/`hw_id`/`img_size`/`crc32` |
| 从机校验 | 每包 CRC16 + 包号；结束整镜像 CRC32（**跳过 32 B 固件头区**）；写头前再校验向量表 |
| CRC 定版 | **CRC-32/ISO-HDLC**（poly 0x04C11DB7 反射 0xEDB88320、init/xorout `0xFFFFFFFF`、refin/refout=true）：`""`→`0x00000000`、`"123456789"`→`0xCBF43926`、单字节 `0x00`→`0xD202EF8D`。包 CRC16 用 **CCITT-FALSE**（`"123456789"`→`0x29B1`）。**两侧必须用示例向量互验** |

### 6.3 错误处理

| 情形 | 行为 |
|------|------|
| 单包超时（1.5 s）/ CRC16 失败 / 包号不符 | 回 `NAK(0x15)`，等重传；连续 ≤6 次 |
| 重传耗尽 / `CAN`×2 | 中止会话回会话建立窗口，LED 慢闪 |
| **主机请求强制回跳** | **仅当固件头与向量表校验通过才跳转**；否则停留升级模式（防止跳进被部分擦除的 App ⇒ 启动即崩 + G3 计数循环） |
| 页擦或半字编程失败 | 立即中止（不回 ACK），LED 快闪 |
| 整镜像 CRC32 失败 | **不写头**，回 `NAK`，可整份重传 |
| `hw_id`/`bl_min_ver` 不符 | 拒绝，回等待窗口 |
| `img_size` 越界 | 立即中止 |

---

## 7. 参数存储 3KB（v3 重构核心）

### 7.1 字段核算（逐字段，剔除从不落盘项）

| 组 | 字段明细 | 字节 |
|----|----------|------|
| K_METER_CAL | `meter_coeff`(4)、`cal_enabled`(4，按 u32 对齐)、`cal_k[7]`(28)、`cal_pct[7]`(28) | 64 |
| K_MEDIUM_COEFF | `medium_coeff` | 4 |
| K_CFG_F | 10×f32：`small_signal`、`filter_time`、`damping_time`、`freq_output`、`medium_density`、`pipe_diameter`、`gas_ref_press`、`gas_ref_temp`、`reynolds_k`、`total_factor` | 40 |
| K_CFG_U16 | 4×u16：`filter_window_count`、`sample_interval_ms`、`modbus_addr`、`oled_recovery_interval` | 8 |
| K_CFG_U8 | 6×u8：`std_cond`、`flow_unit`、`total_unit`、`pulse_equiv`、`uart_config`、`language` | 6 |
| K_TOTAL | `preset_total`（**仅此一项**） | 4 |
| K_DAC | `dac_zero`、`dac_full`（2×u16） | 4 |
| K_SPAN | `value_4ma`、`value_20ma` | 8 |
| **合计载荷** | | **138** |

**明确剔除**（v1/v2 早期误计入）：`forward_total`/`reverse_total`（`param_storage.c:620-631` 只写 RAM）、`pwd_operator`/`pwd_engineer`（无 setter、无 Flash 组）。

### 7.2 页格式：**3 页轮转 + 每页完整镜像**

**核心思路**：不再使用追加日志（无 GC、无压实、无哨兵、无跨页记录），而是**每次提交把整份参数镜像写入一页**，3 页轮转。**自第 3 次提交起**恒有 ≥2 份完整有效镜像（见下方"初始化阶段"说明）。

| 页内偏移 | 内容 | 字节 |
|----------|------|------|
| `0x000` | 页头：`magic u32=0x50504731`("PPG1")、`seq u16`、`state u8=0xA5`、`fmt_ver u8`、`data_crc16 u16`、`hdr_crc16 u16`、`rsvd u32` | 16 |
| `0x010` | K_METER_CAL | 64 |
| `0x050` | K_MEDIUM_COEFF | 4 |
| `0x054` | K_CFG_F | 40 |
| `0x07C` | K_CFG_U16 | 8 |
| `0x084` | K_CFG_U8 | 6 |
| `0x08A` | K_TOTAL + K_DAC | 8 |
| `0x092` | K_SPAN | 8 |
| `0x09A~0x0FF` | 保留（`0xFF`） | 102 |
| `0x100` | **BL 通信配置槽**：`magic u32="BLCF"`、`uart_config u8`、`rsvd[3]`、`slot_crc16 u16`、`rsvd[6]` | 16 |
| `0x110~0x3FF` | 保留（`0xFF`） | 752 |

**页头 CRC 覆盖范围（v3.1 修正，避免前向兼容陷阱）**：
- **页头**（区别于 §4.5 的**固件头**）CRC 覆盖范围（v3.1 修正，避免前向兼容陷阱）：
  - `hdr_crc16` 覆盖页头 `0x000~0x009`，即 **`magic`、`seq`、`state`、`fmt_ver`、`data_crc16`**（不含 `hdr_crc16` 自身与 `rsvd u32`）；
- `data_crc16` **只覆盖 `0x010~0x10F`**（实际使用的参数区 + BL 槽），**不覆盖 `0x110~0x3FF` 保留区** —— 否则将来固件启用保留区后，旧固件复算 CRC 必然失败 ⇒ 误判"三页全无效"⇒ 误触发重迁移并用旧备份覆盖新值（降级为数据丢失）。

**页有效性** = `magic` 正确 **且** `state==0xA5` **且** `hdr_crc16` 通过 **且** `data_crc16` 通过。

**提交目标选择（v3.1 修正，原"选 seq 最小页"不完整，可能擦掉最新有效页）**：
1. **优先选"无效页"**（任一校验不通过，含未初始化/半擦页）—— 这样垃圾 `seq` 永远不会参与比较；**若存在 ≥2 个无效页，取物理页序号最小者**（与下面第 3 条一致）；
2. 三页全有效时，选 `seq` **最小**者，比较用 **`(int16_t)(a-b) < 0`**（处理 u16 回绕，v2 有、v3.0 遗漏，属回归，此处补回）；
3. `seq` 相同（罕见）⇒ 选**物理页序号最小**者；
4. 目标页选定后：擦页 → 写入完整页镜像（**含从 RAM 缓存重写的 BL 通信槽**）→ `seq = 当前最大有效 seq + 1`。

**读取**：读 3 页页头（各 16 B）→ 取有效页中 `seq` 最大者。**不改写任何东西。**

- **掉电安全**：提交中掉电只损坏"正在写的那一页"，其余有效页不受影响 ⇒ 下次上电读到上一份完整镜像，**零数据丢失**（优于 v2 的日志，也优于方案 B 的单页 RMW）。
- **初始化阶段的限定（v3.1 修正）**：**第 1 次提交期间有效镜像数为 0、第 2 次期间为 1**，此时掉电会失去唯一有效镜像 —— 这正是 §7.3 必须**备份全部 10 页**的原因；恢复路径 = Page 8 备份 + 旧 61~63 迁移。第 3 次提交起才恒有 ≥2 份。
- **BL 通信槽必须每次提交都从 RAM 缓存重写**（v3.1 修正）：槽在页内，整页提交会连它一起重写；若实现成"仅配置变更时才写槽"，下一次任意提交就会把槽写成 `0xFF` ⇒ 冷启动 BL 读不到槽 ⇒ 回落 115200，而非默认波特率设备**失联**（正是用户点名的链条）。配 T-15 扩展用例。
- **磨损与寿命（v3.1 按 R2 建议降额）**：每次提交 1 次页擦，均摊 3 页 ⇒ 理论 `3 × 10,000 = 30,000` 次；此值**恰好等于每页 10 kcycle 保证下限、零余量**，故**按 50% 降额报 15,000 次提交** ≈ **1,000 次标定会话**（14 次 FC06/会话），不再写"充裕"。
- **提交耗时与看门狗（v3.1 修正）**：擦 30~40 ms + 写 272 B（136 半字 × ~60 µs ≈ 8 ms）⇒ **约 40~50 ms**。**App 当前 IWDG 仅约 100 ms，提交会吃掉近一半窗口**（提交期间主循环无法喂狗）⇒ 必须：① **把 App 的 IWDG 放宽到约 1 s**（`IWDG_PRESCALER_64` + Reload 624 ⇒ 64×625/40 kHz ≈ 1.0 s）；② 提交前后各喂一次狗。放宽 IWDG 同时消除了"暖复位后 100 ms 看门狗"的隐患（§4.6）。
- **延迟提交（合并写放大）**：setter 只更新 RAM 缓存并置 `dirty`；`param_store_commit()` 在**每个 Modbus 写帧结束**与**每个菜单保存动作结束**各调用一次 ⇒ 一帧内多个 setter 只产生 1 次提交（现状代码每个 setter 立即 `flush_*_group()`，一次 FC10 写标定区会产生 7 次写）。
- **`fmt_ver` 与缺失字段规则（R2 备注④落地，列为"实现前必改"）**：`fmt_ver` 不匹配 ⇒ **不做整体拒绝**，改为逐字段兼容解析；保留/未用字节为 `0xFF` 视为**字段缺失** ⇒ 取默认值并置 `PARAM_STATUS` bit2；**缺失字段绝不得触发重新迁移**（否则会用旧备份覆盖新值）。

### 7.3 参数迁移（跨版本，必须做）

**问题**：新 App 区 `0x08002400~0x0800F3FF` 覆盖旧 Page 54~60；新参数页 61~63 正是旧 METER/MEDIUM_COEFF/SPAN 页。

| 旧页 | 命运 | 数据价值 |
|------|------|----------|
| 54~60（DISPLAY/SIGNAL/OUTPUT/MEDIUM_PARAM/SYSTEM/DAC/BASIC） | 位于新 App 区内 ⇒ **升级时被物理擦除** | 地址、波特率、单位、密度、管径、DAC 校准等 |
| 61~63（METER+CAL / MEDIUM_COEFF / SPAN） | 物理存活（不在 App 区内） | **标定、量程**（最关键） |

**迁移路径（两路互补，现场零人工）**：

1. **BL 侧备份 —— 必须覆盖全部 10 页（54~63，152 B）**（v3.1 修正；v3.0 只备份 54~60 是错的）：
   - **为什么必须全量**：新 store 的 3 页**就是 61~63**，而"第 1/2 次提交期间有效镜像数只有 0/1"（§7.2），此时若掉电：RAM 副本丢失 + store 三页全无效 + 被擦那一页的旧数据同时被毁 ⇒ 该组（含**标定/量程**）**永久丢失** —— 而 Page 8 备份里若没有它就无从恢复。
   - 做法：用共享的 `legacy_param_read.c` 抽取 Page **54~63** 的活数据（旧 append-log 语义），写入 **Page 8 备份块**（244 B）：
     `{magic u32="BLK1", ver u8, legacy_fmt_id u8, n_groups u8=10, src_epoch u8, group_desc[10]×4B, payload 152 B, rsvd→240 B, crc32}`（`src_epoch`：0=旧 10 页抽取，1=由新 store 刷新）。
   - 写入后**回读 + CRC32 + 逐字段区间校验**（`cal_k∈[0.5,2.0]`、`cal_pct` 单调不减且 ∈`[0,100]`、`modbus_addr∈[1,247]`、`baud idx ≤5`、`sample_interval_ms∈[100,60000]`、`filter_window_count∈[2,10]`、各 float 非 NaN/Inf 且在 `param_storage.c` 的 MIN/MAX 内）。
   - **抽取前置条件**：① 当前固件头无效（App 有效则 Page 54~60 已是 App 代码，抽取必为垃圾）；② 尚无"已就绪"的备份；③ 区间校验通过。**备份一旦就绪即永不重抽**。
   - **备份未就绪 ⇒ BL 拒绝一切擦除类命令**，保持旧 App 完好（"宁可不升级，也不丢标定"）。
2. **App 侧迁移**（`param_storage_init()` 内，幂等）：
   - 若新格式有效（3 页中至少 1 页 `magic+state+hdr_crc16+data_crc16` 通过）⇒ 直接载入；
   - 否则：**先把全部数据源读入 RAM**（顺序不可颠倒）：① 用 `legacy_param_read` 读旧 Page **61~63**（标定/介质系数/量程）；② 用 Page 8 备份块补齐 Page **54~60** 的值（逐字段：已有值不覆盖，仅补缺失）；
   - 两者皆无 ⇒ 取默认值，并置 `PARAM_STATUS` 相应位；
   - **然后才提交新格式**（此时 61~63 的旧值已全部在 RAM 中，提交擦页安全）⇒ 提交成功才算迁移完成；任一环节掉电，下次上电重跑（Page 8 备份不再被擦，数据源始终可用）；
   - 结果按"完整/部分/全默认"记录到 `PARAM_STATUS`，**部分迁移时必须显式告警**（OLED + 寄存器位），并**禁止自动改写 `modbus_addr`/`uart_config`**（避免设备失联）。

### 7.4 迁移后的收益

- **BL 与 App 的存储接口只剩 16 B 固定槽位** —— 该结构在 M1 冻结即可，**不需要冻结整卷格式**（用户提醒 #3 的复杂度点被消除）。
- 无日志、无 GC、无压实 ⇒ 无 v2 中"半擦页被当空闲页""死页""停止/跳过语义"这一整类缺陷。

---

## 8. 上位机（工具链）

| 项 | 内容 |
|----|------|
| 形态 | **Python 脚本 `tools/ufl_update.py`（首选）**：复用已有 `serial-monitor` / `modbus-debug` 技能生态，落地快、易版本管理；WPF 升级页作为后续客户界面（可选，+3~4 人天） |
| 功能 | ① 连接与识别（读 `40128~40129` 版本、`40131` 参数状态）；② 镜像预检（§6.2）；③ 触发升级（写 `40127=0x5AA5`，或提示手动复位）；④ XModem-CRC 发送（1K 包、进度条、重传计数）；⑤ 升级后校验版本；⑥ **参数一致性报告**：升级前后各做一次全量寄存器快照（`40001~40131`）并 diff |
| 取消机制 | 必须提供"取消"，取消后**请求立即回跳**（`CAN`×2）。注意：BL **仅在固件头 + 向量表校验通过时**才立即跳转；若 App 区已被部分擦除（头无效），BL 会**停留升级模式**并提示"需重新升级" —— 这是刻意设计，避免跳进残缺镜像造成启动即崩的复位循环（配 T-20） |
| 风险提示 | 升级前明确提示：4~20mA 输出无效、OLED 停止刷新、Modbus 常规寄存器不可读、请勿断电 |
| 兼容 | 保留现有全部 Modbus 功能；升级功能独立入口 |

---

## 9. 失效模式与安全性分析（FMEA）

| 场景 | 后果 | 检测 | 恢复 |
|------|------|------|------|
| 升级中掉电 | 固件头未写 ⇒ App 判定无效 | 层 1 校验 | 重入升级模式，RS-485 重新升级（**无需 SWD**） |
| 升级中主机断连/中止 | BL 保持等待 | 15 s 超时 | 重连重来；App 有效则自动跳回 |
| 传输误码 | 单包 CRC16 失败 | 包级 CRC + 整镜像 CRC32 | NAK 重传 / 整份重传 |
| **参数提交中掉电（自第 3 次提交起）** | 仅"正在写的那一页"损坏 | 另有 ≥2 页完整镜像 | **零丢失**，下次上电读最新有效页 |
| **参数提交中掉电（初始化阶段：第 1/2 次提交）** | 有效镜像数为 0/1，被擦页的旧值同时被毁 | 页头校验 + Page 6 备份 | 由**全量 10 页备份 + 旧 61~63 迁移**重建（这正是"必须备份全部 10 页"的原因）；配 T-39 |
| **首次升级掉电（备份已就绪）** | App 残缺，**标定与配置安全** | 固件头 | 重新升级 |
| **首次升级掉电（备份未就绪且已开始擦除）** | 不可能：未就绪时 BL 拒绝擦除 | 错误码 | 旧 App 完好 |
| 备份页副本写坏 | 回读校验失败 | CRC32 + 区间校验 | 重写（仅在未就绪状态） |
| 迁移中断 | 新格式不完整 | 3 页均无有效页头 | 幂等重跑（数据源仍在） |
| 部分迁移 | 部分字段为默认 | `PARAM_STATUS` bit1 + OLED | 人工确认后重配置；不改地址/波特率 |
| 固件头通过但 App 启动即崩 | 反复重启 | G3 启动计数（RAM 邮箱） | 连续 3 次非零 ⇒ BL 停留升级模式 |
| 暖复位后残余看门狗（**原固件 100 ms ⇒ v3.1 放宽到约 1 s**） | App 启动期若长于该值 ⇒ 复位循环 | — | 跳转前放宽至 5 s + App `main()` 开头原始喂狗 + T-32；App IWDG 已在 M1 放宽到 1 s |
| 看门狗在长会话中误复位 | 升级中断 | — | 会话期 5 s + 多点喂狗；复位后重入升级模式 |
| BL 自身损坏 | 只能 SWD | — | BL 区永不擦写：BL 自身的擦写白名单 = {App 区 `0x08002400~0x0800F3FF`} ∪ {**Page 6，仅备份未就绪时**}，越界即中止（**WRP 在本分区不可用**，4 页/位粒度，见 §10.3） |
| **误烧旧全片镜像** | **BL + 参数区全毁** | — | §10.3 治理措施（仓库/烧录/选项字节） |
| 升级期间输出 | DAC/PWM 停 ⇒ 4~20mA 无效；OLED 停帧；Modbus 常规寄存器不可读；IWDG 由 App 的约 1 s 放宽到 BL 会话期的 5 s | — | 工具提示 + 运营说明；**UFL-1A 侧累积量不受影响**（`Cumulativeflow` 来自模块 BCD 帧） |
| 会话空闲窗口 | 主机故障后最长 **≈24 s（115200）/ ≈31 s（9600）** 不可用，**期间无本地可视指示**（OLED 停帧） | — | 工具主动取消可立即回跳（需头校验通过）；窗口数值写入运营说明（§10.1） |

---

## 10. 业务与治理（用户提醒 #1 的直接回应）

### 10.1 不可用窗口的量化与压缩

| 版本 | 窗口（正常） | 窗口（异常/中止） | 说明 |
|------|--------------|-------------------|------|
| v2 | 20~30 s | 60 s | Modbus IAP，每帧需往返 |
| **v3** | **8~10 s** | **≤25 s @115200 / ≤35 s @9600**（6 次 NAK 耗尽 + 15 s 建立窗口） | XModem-1K，46 包 |

仍然存在的硬事实：**窗口内 4~20mA 输出无效、测量中断、无自动回滚**。这是单 Bank 64KB 且无外部 Flash 的物理限制，无法通过设计消除，只能压缩与安排（升级排在计划停机窗口，工具强制提示）。

### 10.2 无自动回滚的补偿

- **G3 启动计数**：连续 3 次"跳转后未收到 App 存活确认"⇒ BL 停留升级模式（避免"CRC 通过但启动即崩"的死循环）。
- **升级前后全量寄存器快照 diff**（§8）作为量化验收，参数一致性可追溯。
- **不做**：加密/签名、A/B 双区、BL 内 OLED、页内多槽磨损均衡（无收益）。

### 10.3 "误烧旧全片镜像即毁 BL+参数区"的治理措施（Q9/R15）

| 层面 | 措施 |
|------|------|
| 仓库 | **删除或归档 `0x08000000` 全片旧镜像**；唯一量产产物 = `merged.bin`（BL+App）；另存 `boot_only.bin` 作为恢复镜像；README 首屏警示 |
| 烧录 | 量产脚本必须校验"仅写指定页范围 / 仅按段擦除"；禁止 `--chip-erase`；下载配置（`.ewd`）纳入版本控制 |
| 选项字节 / WRP | **结论：不采用（G1-d 修订后已于 v3.2.1 重评）**。v3.1 原论据（4 页/位粒度下护 6KB BL 必连带保护备份页 6 与 App 页 7 ⇒ 不可用）在 G1-d 新分区下**失效**——BL 恰为页 0~7，`WRP_PAGES0TO3 ∪ WRP_PAGES4TO7` 可精确覆盖 BL 且不连带任何其它分区。但仍不采用，理由改为：① §5.5 明令禁止任何 OB/RDP 写入（选项字节操作与 RDP 变更触发 mass erase 的风险面耦合）；② WRP 设置/解除均需 SWD，与"BL 区仅 SWD 写入"的既有治理等价但多一道出厂工序；③ 代码内白名单硬边界（下述两段式）已覆盖误擦写防护。**结论降级为：量产可选增强（Q5 保留），默认关闭**。**BL 允许擦写的地址白名单必须含两项**（v3.1 修正 —— 早期只写 App 区会把备份页排除，导致备份永远建不起来 ⇒ 永久拒绝升级）：
   ① **App 区** `0x08002400~0x0800F3FF`（升级主体）；
   ② **Page 8 `0x08002000~0x080023FF`，且仅在"备份未就绪"状态下允许**（用于写入旧参数备份块，见 §7.3）。
   其余地址一律立即中止。**注意 App 侧守卫是另一套规则**（App 只允许写 `0x0800F400~0x0800FFFF` 参数区，见 §5.4），两套阈值不可混用。治理措施见本节其余各行 |
| 交付物 | M6《首次现场切换与回滚规程》含"误烧恢复"一节（用 SWD 重烧 `merged.bin`） |

---

## 11. 实施计划

| 阶段 | 内容 | 人天 |
|------|------|------|
| **M0** | `0x1FFFF7E0` 容量核查；BOOT0/PA13/PA14/PA9/PA10 可达性；PA0 上电电平与按键冲突验证（`main.h:69-70`）；IAR Size 优化体积实测；**§2.6 业务前提拍板**；现场切换规程初稿 | 1.5 |
| **M1** | `param_store.c/h`（3 页轮转/页格式/提交/延迟提交/守卫）；`legacy_param_read.c/h`（兼容 Page 54 的 `Len=1`/`Len=2` 两代）；迁移（幂等、`PARAM_STATUS`、部分迁移告警）；10 处调用点改造；`Data_Init()` 重写；**M1 冻结 16 B BL 槽 + 页头格式**；通过 T-12/T-13/T-14/T-30/T-35/T-37/T-39 | 5 |
| **M2** | `UMF_Boot.ewp` + 两套 ICF；`.fw_header` 保留区 + IAR Post-build 注入头（或 BL 写入路径）；App 迁 `0x08002400` + `USER_VECT_TAB_ADDRESS`；邮箱（含宽度/volatile/CRC32/boot_attempt）；**BL 首次会话即备份 + 回读 + 区间校验 + 拒绝擦除**；SWD 分别烧录验证；G1 体积粗判 | 4 |
| **M3** | XModem-CRC 状态机（1K/128B、重传、超时、CAN）；CRC32/CRC16 定版与双向测试向量；RS-485 DE 半双工时序；IWDG 5 s；固件头写入与分层校验；恢复模式；T-07/T-08/T-19/T-22/T-23/T-26/T-36 通过 | 5 |
| **M4** | `tools/ufl_update.py`（预检/发送/重传/取消强制回跳/参数快照 diff）；`.bin`+`.json` 后构建定版；联调；T-04/T-05/T-06/T-21/T-25 通过 | 3 |
| **M5** | §12 全量用例（掉电/误码注入、长期稳定性、T-27/T-28/T-30/T-31/T-32/T-33/T-34/T-38）；运营说明；治理措施落地 | 4 |
| **M6** | 《首次现场切换与回滚规程》：参数快照模板、只烧 Page 0~5 步骤、RS-485 升级验证清单、失败回滚、误烧恢复、单台工时标定与 200+ 台排期；现场演练 | 2~3 |
| | **合计** | **24.5~25.5（报 25~30）** |

> v3 比 v2 少 3 人天：XModem 取代 Modbus IAP（BL 与工具都更简单）、取消日志/GC、取消描述符页管理、BL 侧扫描器降为 16 B 槽位读取。

---

## 12. 测试与验收

### 12.1 测试用例

| 编号 | 用例 | 期望 |
|------|------|------|
| T-01 | App 在 `0x08002400` 全功能回归 | 与迁移前一致 |
| T-02 | VTOR 重定位后 USART1/2、TIM3 中断正常 | 无跑飞 |
| T-03 | BL 冷启动且 App 有效 | 冷启动 <150 ms（含层 3）；暖复位 <100 ms |
| T-04 | App 写 `40127=0x5AA5` | 复位进升级模式 |
| T-05 | 完整升级 @115200（1K 包） | 成功；**记录实测耗时（预期 8~10 s）** |
| T-06 | 完整升级 @9600 | 成功；记录耗时（预期 ≤60 s） |
| T-07 | 单包重传同数据 | 无 PGERR，最终 CRC 通过 |
| T-08 | 内容篡改（包 CRC16 正确） | 整镜像 CRC32 失败，不写头 |
| T-09 | 升级 50% 断电 ×20 | 每次可重新升级成功 |
| T-10 | 写固件头瞬间断电 ×20 | App 判无效，可重新升级 |
| T-11 | 会话中触发看门狗 | 复位后重入升级模式 |
| T-12 | 参数提交途中断电 ×20（3 页轮转） | **自第 3 次提交起零丢失**：始终能读到上一份完整镜像（初始化阶段另见 T-39） |
| T-13 | 升级前后**全量逐字段**快照 diff（**138 B 载荷**） | 全部相等；`forward/reverse` 标注"按设计不比对" |
| T-14 | 3 万次参数提交 + 随机断电 | 无损坏；统计实际擦除次数验证磨损模型；**并断言"全程无看门狗复位"**（App IWDG 已放宽到 1 s，提交 40~50 ms） |
| T-15 | ① 用户改过波特率（9600）后冷启动升级；② **改波特率 → 再改一个无关参数 → 掉电 → 冷启动升级** | BL 从固定槽位读到 9600 并成功；② 验证"每次提交都从 RAM 重写槽位"，槽值不被后续提交抹成 `0xFF` |
| T-16 | 固定槽位不可用 + 冷启动 | 回落 115200 8N1 并成功 |
| T-17 | 错误 `hw_id`/版本镜像 | 拒绝，停留升级模式 |
| T-18 | 升级中 4~20mA/OLED/Modbus 行为记录 | 形成运营说明 |
| T-19 | 连续升级 30 次 | 全部成功 |
| T-20 | 会话中止/断连 | 主机故障后最长 ≈24 s（≤25 s）且 App 有效则自动跳回；工具"取消"：**固件头有效才立即跳回**，头无效则停留升级模式 |
| T-21 | 主机-从机协议版本不匹配 | 握手前拒绝并提示 |
| T-22 | 镜像长度边界：511/512/1023/1024/53,248/53,249/奇数/0 | 非法长度拒绝；页边界正确 |
| T-23 | 连续断连重试（每 10 包断 100 ms ×50） | 不误判、不跳残缺 App、最终 CRC 通过 |
| T-24 | RS-485 总线噪声/多从站（同总线另有 Modbus 从站，或注入 `0x01`/`0x02` 开头的杂帧） | 误判只导致 NAK 与重传，**不写错数据**；**要求升级期间总线静默 / 点对点直连**（§6.1）；总线持续繁忙时会话中止并回到等待窗口 |
| T-25 | 地址/波特率改动后冷启动可升级 | 不得出现"必须 SWD 才能救回" |
| T-26 | 备份未就绪时下发升级 | 拒绝并保持旧 App 完好 |
| T-27 | "不会变砖"专项：① App 区全擦 ② 半擦+头无效 ③ SWD 误擦 App 区，各 20 次 | 均可 RS-485 恢复 |
| T-28 | 固件头与向量表同页：头重写不影响向量表 | 跳转正常；读回校验一致 |
| T-29 | 恢复模式（PA0）可用性 | 长按 ≥3 s + 二次确认才进入 |
| T-30 | 参数页全 `0x00` / 全 `0xFF` / 单页损坏 | 正确定位到有效页或走迁移，不误判 |
| T-31 | （若 G1-d 触发）无备份页方案 | 按重新评审结论执行 |
| T-32 | 暖复位 100 次"App → BL → App" | 无复位循环 |
| T-33 | 跳转后中断使能验证（SysTick/`HAL_Delay`、TIM3、USART1/2） | 全部正常 —— 专测"漏写 `__enable_irq()`"类缺陷 |
| T-34 | 层 3 整镜像 CRC32 耗时实测 | 记录实测；>150 ms 则重新评估 |
| T-35 | 存储层守卫：向 `< 0x0800F400` 擦/写 | 被拒绝，App 不受影响 |
| T-36 | PA0 误触发：上电按住 1 s / 2 s / 3 s 各 20 次 | 仅"≥3 s 且二次确认"进入恢复模式；计量不中断 |
| T-37 | 页擦除中断产生的半擦页 ×20 | 该页 CRC 失败被跳过；不影响另 2 页 |
| T-38 | 迁移前置条件：App 有效时不得重抽备份 | `src_epoch` 不变，备份不被覆盖 |
| **T-39** | **初始化阶段掉电**：第 1 次提交（3 页全空）与第 2 次提交中掉电，各 ×20 | 由 Page 8 备份 + 旧 61~63 完整恢复（标定/量程逐一比对）；**这是"必须全量备份 10 页"的验证用例** |
| **T-40** | 固件头构建产物校验：① `len ≥ 0x220` 且偶数；② `bin[0x200..0x203] == "UMFH"`；③ `bin[0x214..0x21F]` 全 `0xFF`；④ `word0/word1` 合法 | 全部通过；**人为破坏任一项 ⇒ 构建失败**（防"升级成功但永不启动"） |
| **T-41** | 槽位语义校验：注入 `baud_idx=7`/`parity=3`/`stop=2` 的非法槽值 | 判为无效槽并回落缺省 115200 8N1，不配出非法波特率 |
| **T-43** | **SOH/128 B 包型**：主机以 128 B 包（SOH）发送完整镜像 | 成功；与 1K 包型结果一致（§12.2 的"双包型实机通过"） |

### 12.2 验收门禁

1. 分区与 §3 逐字节一致；两套 ICF 边界即断言，越界 ILINK 报错；
2. BL ≤ 8,192 B（G1-d 修订区），XModem 1K/128B 双包型实机通过；App ≤ 53,248 B 且余量 ≥4KB；
3. **强制门禁**：T-09、T-10、T-12、T-13、T-14、T-19、T-22、T-26、T-27、T-30、T-32、T-33、T-35、T-36、T-37、T-38、**T-39、T-40** 全部通过，掉电类无例外；
4. T-13 必须**逐字段**通过；
5. IAR 零 warning 零 error；
6. `README.md`、`CLAUDE.md`、`UMF_Modbus_Protocol.md`、`EWARM` 说明同步更新（含 §10.3 治理条款）。

---

## 13. 待确认问题

| 编号 | 问题 | 为什么重要 |
|------|------|-----------|
| Q1 | `0x1FFFF7E0` 实测值？ | 决定是否可放宽分区 |
| Q2 | BOOT0、PA13/PA14 现场可达性？ | SWD 回滚与兜底是否可行 |
| Q3 | 是否接受升级期间 4~20mA 中断（**正常 8~10 s；主机故障时最长 ≤25 s @115200 / ≤35 s @9600**）？ | 工艺联锁 |
| Q4 | 是否接受 PA0 长按 ≥3 s + 二次确认作为最后恢复手段？ | §4.7 优先级 5 可行性 |
| Q5 | 是否接受"**本分区无硬件写保护（WRP 不可用）**，BL 不可擦写性仅由代码硬边界 + 治理措施保证"？（见 §10.3 的证据与替代方案） | 决定误操作防护的强度 |
| Q6 | 上位机升级功能的开放与权限策略？ | 误操作风险 |
| **Q7** | **升级需求的业务性质**：远程/自助升级是必需项，还是仅需把 200+ 台设备升级一次？ | **决定选 IAP 还是 SWD（§2.6）；若为后者，建议本方案暂缓，可省 25~30 人天与全部 C1/C2/C3 风险** |
| Q8 | 是否实施 G3（RAM 邮箱启动计数 + 连续 3 次自动回退）？ | "启动即崩"能否自动恢复；约 +1 人天 |
| Q9 | 是否接受"仓库内旧 `0x08000000` 全片镜像作废/归档 + `.ewd` 纳入版本控制"？ | 误烧一次即毁 BL 与参数区（§10.3） |
| Q10 | PA9/PA10 是否引出到接插件、是否与 UFL-1A TX 共线？ | 决定 USART1 能否作为**备用**升级通道（v3 默认走 RS-485） |

---

## 附录 A：地址总表

| 名称 | 地址 | 大小 | 归属 |
|------|------|------|------|
| Bootloader | `0x08000000~0x08001FFF` | 8,192 B | BL 工程 |
| 旧参数备份页 | `0x08002000~0x080023FF` | 1,024 B | 仅 BL 写 |
| **固件头** | `0x08002600~0x0800261F` | 32 B | 升级时 BL 写 |
| App 向量表/代码 | `0x08002400~0x0800F3FF` | 53,248 B | App 工程 |
| 参数存储（3 页轮转） | `0x0800F400~0x0800FFFF` | 3,072 B | App 写 |
| RAM 链接区 | `0x20000000~0x20004BFF` | 19,456 B | 两工程各自链接 |
| 共享邮箱 | `0x20004C00~0x20004DFF` | 512 B | 两侧均不链接 |
| 保留 RAM | `0x20004E00~0x20004FFF` | 512 B | 预留 |

## 附录 B：关键常量

- App 基址 `0x08002400`；`VECT_TAB_OFFSET = 0x2400`；固件头 `App+0x200`
- 固件头 magic `0x554D4648`("UMFH")；页头 magic `0x50504731`("PPG1")；BL 槽 magic `0x424C4346`("BLCF")；邮箱 magic `0x424C4F54`("BLOT")
- CRC32 = CRC-32/ISO-HDLC（`"123456789"`→`0xCBF43926`）；CRC16 = CCITT-FALSE（`"123456789"`→`0x29B1`）
- IWDG 会话期：`IWDG_PRESCALER_64`（枚举值 0x04）+ Reload 3124 ≈ 5.0 s，改后必须写 `KR=0xAAAA` 重载
- XModem：`SOH=0x01`/`STX=0x02`/`EOT=0x04`/`ACK=0x06`/`NAK=0x15`/`CAN=0x18`/`'C'=0x43`

## 附录 C：v3.0 相对 v2.0 的变更清单

| 项 | v2.0 | v3.0 | 来源 |
|----|------|------|------|
| 防砖标记 | 独立描述符页 + INVALID 先行 | **固件头内嵌 App、最后写** | 方案 B |
| App 区 | 页 8~60（53KB） | **页 7~60（54KB，余 9,128 B）** | 采纳 B 省页 |
| EEPROM | 记录型日志 + 三页 GC | **3 页轮转 + 每页完整镜像 + seq/CRC** | 方案 B 思路 + v3 强化 |
| BL 通信参数 | 400 B 日志扫描器（"失联"链上） | **16 B 固定槽位（1 字节 `uart_config`）** | 用户提醒 #3 |
| 传输 | Modbus IAP（需从站地址） | **XModem-CRC over RS-485（无地址依赖）** | 方案 B + 去地址化 |
| 窗口 | 20~30 s / 60 s | **8~10 s / ≤25 s @115200（≤35 s @9600）** | XModem-1K |
| 工时 | 27.5~28.5 人天 | **24.5~25.5 人天** | 复杂度下降 |
| 治理风险 | 仅列为风险 | **§10.3 可执行治理措施（仓库归档/量产脚本禁整片擦除/`.ewd` 纳入版本控制）；WRP 经证据判定在本分区不可用** | 用户提醒 #1 |
| 业务前提 | §2.6 + Q7 | **§2.6 决策表 + Q7 明确"先拍板"** | 用户提醒 #1 |

---

*本方案所有"实测"数据均来自本仓库构建产物与源码核对并标注出处；"估算"体积必须由 M2/M3 真实编译结果替换。*
