# UMF 固件 Bootloader 升级方案设计

> 状态: **已通过第三方 agent 审查（PASS WITH CHANGES），所有必须修改项已并入本文档**
> 日期: 2026-09-17（v1.1，按审查报告修订）
> 硬件约束: STM32F103C8T6（64KB Flash / 20KB RAM，不可更换芯片）
> 用户决策: Flash-EEPROM 区由 10KB 缩减为 **3KB**

## 1. 背景与目标

在现有 UMF 固件（无 RTOS、裸机超级循环）上增加 Bootloader 在线升级能力，实现：

1. 通过串口（XModem）升级应用固件，无需拆机、无需 SWD 调试器
2. 升级中途断电不砖——Bootloader 常驻低区 Flash，任何情况下可重新升级
3. 应用固件（App）重定位，Bootloader（BL）与 App 分区隔离

## 2. 资源实测基线（来自 `EWARM/UMF/List/UMF.map`）

| 项目 | 数值 | 说明 |
|------|------|------|
| App Flash 占用 | **46,168 B**（代码 36,533 + 只读数据 9,635） | ≈ 45.1KB，注意 CLAUDE.md 中 "~34KB" 已过时 |
| App RAM 占用 | **5,463 B**（已含 CSTACK 2KB；HEAP 未分配，纯 rw 3,415B） | ≈ 5.3KB，空闲 ≈ **15KB** |
| 主要 Flash 大户 | bsp_usart 8.2K / bsp_menu 8.0K / 字体 6.9K / HAL 9.1K | 供后续瘦身参考 |
| 读保护 RDP | 未使用 | SWD 始终可用，是最后的恢复手段 |
| 选项字节 HW-Watchdog | **待确认**（前置确认项 #3） | 若已使能则必须落实 §5.8 防御 |

## 3. 总体 Flash 布局（三区划分）

F103C8T6 单 Bank 64KB，页粒度 1KB，复位固定从 `0x08000000` 启动 → BL 必须占 Page 0 起。

```
0x08000000 ┌─────────────────────────────┐
           │  Bootloader  4KB (Page 0~3) │  ← 常驻，永不擦除
0x08001000 ├─────────────────────────────┤
           │  App         57KB (Page 4~60)│  ← 升级时整体擦写
0x0800F400 ├─────────────────────────────┤
           │  EEPROM       3KB (Page 61~63)│ ← 参数/标定/量程，升级不触碰
0x08010000 └─────────────────────────────┘
```

| 分区 | 地址范围 | 大小 | 当前占用 | 余量 |
|------|----------|------|---------|------|
| Bootloader | 0x08000000~0x08000FFF | 4KB | 目标 ≤ 2.8KB 代码 | ~1.2KB |
| App | 0x08001000~0x0800F3FF | 57KB | 45.1KB | **~11.9KB** |
| EEPROM | 0x0800F400~0x0800FFFF | 3KB | 实际数据 ~156B | 充裕 |

**RAM 分析（无压力）**: BL 在 App 之前独立运行，不叠加占用。BL 自身需求：栈 512B + XModem 接收缓冲 128B + 变量 < 200B ≈ **< 1KB**；App 空闲 RAM ~15KB 完全不受影响。

## 4. EEPROM 3KB 重打包设计（前置任务 P0）

### 4.1 现状

`eeprom.c` 为 append-log 模拟 EEPROM：**每页只容纳一条记录流**（头字 0x00000000 + 数据，页满擦除整页重写）。10 个参数组 = 10 页：

| 旧页 | 组 | 记录大小 |
|------|-----|---------|
| 54 | 显示/滤波/采样 (2w) | 12B |
| 55 | 信号处理 (3w) | 16B |
| 56 | 输出配置 (3w) | 16B |
| 57 | 介质工况 (5w) | 24B |
| 58 | 系统/累积 (4w) | 20B |
| 59 | DAC 校准 (2×u16) | 6B |
| 60 | 基本参数 (1w) | 8B |
| 61 | 仪表系数+七点标定 (16w) | 68B |
| 62 | 介质系数 (1w) | 8B |
| 63 | Span 量程 (2w) | 12B |

全部有效数据仅 ~156B，10 页纯属页粒度浪费。

### 4.2 新布局：固定偏移 + 页内读改写（RMW）

每页多组固定偏移存放，保存某组时：读整页到 RAM 镜像 → 更新目标组 → 擦页 → 整页写回。

| 新页 | 偏移 | 组 | 大小 |
|------|------|-----|------|
| Page 61 (0x0800F400) | +0x00 | 仪表系数+标定 (16w) | 64B |
|  | +0x60 | 介质系数 (1w) | 4B |
|  | +0x70 | 基本参数 (1w) | 4B |
| Page 62 (0x0800F800) | +0x00 | 信号处理 (3w) | 12B |
|  | +0x20 | 输出配置 (3w) | 12B |
|  | +0x40 | 介质工况 (5w) | 20B |
|  | +0x60 | 系统/累积 (4w) | 16B |
| Page 63 (0x0800FC00) | +0x00 | 显示/滤波/采样 (2w) | 8B |
|  | +0x20 | DAC 校准 (2×u16) | 4B |
|  | +0x30 | Span 量程 (2w) | 8B |

新 `eeprom.c` API（保留底层 HAL_FLASH 原语，替换上层流式逻辑）：

```c
HAL_StatusTypeDef page_read (uint32_t page_addr, uint32_t mirror[], uint32_t words); /* 整页读 */
HAL_StatusTypeDef page_write(uint32_t page_addr, const uint32_t mirror[], uint32_t words); /* 擦+写整页 */
```

`param_storage.c` / `bsp_usart.c` / `bsp_menu.c` / `main.h` 中所有 `ADDR_FLASH_PAGE_xx` 常量改为新页+组偏移宏；每个 setter 内部走 "page_read → 改组 → page_write"。

- **磨损评估**: 每组保存擦除整页一次；菜单配置频度低，10k 次擦除寿命足够。**注意突发模式**: 一次标定会话 = k[0..6]+pct[0..6] 共 14 次 FC06 = 14 次整页擦除（Page 61），若 P0 后实测磨损偏快，再评估页内双槽轮换（YAGNI，暂不做）
- **RMW 断电窗口**: 擦页后写回前断电 → 该页全部组归默认值。旧 append-log 页满擦除时存在同类窗口，性质相当；P4 加固期评估"页级提交标记"（第 0 字写页提交号，读时校验）
- **陈旧副本清理**: `Drivers\CMSIS\Src\main.c` / `Drivers\CMSIS\Inc\main.h` 是不参与编译的旧副本（含旧页地址常量），P0 迁移替换时必须排除误扫，建议直接删除或加"已废弃"注释

### 4.3 旧格式启动迁移（必须实现，不能只靠上位机备份）

旧 append-log 格式的头字为 `0x00000000`——若 P0 直接上固定偏移读取，旧 Page 61 的头字会被误读为 `meter_coeff = 0.0`（不是 0xFFFFFFFF 空哨兵）→ clamp 到 0.001 → **仪表系数被静默改成 0.001，DAC 输出趋零**；首次保存参数时旧标定数据被覆盖销毁。

**对策**: P0 固件启动时检测——新格式无效（哨兵 0xFFFFFFFF 或合理性检查失败）且旧格式记录存在（append-log 头字 0 且后继记录有效）时，读取旧值并立即写为新格式。代码库已有同款先例（`BSP/param_storage.c:281-327` 新旧双格式迁移），照搬模式即可。

### 4.4 数据迁移与备份/恢复（上位机工具职责，P3）

首次升级时旧参数页的实际命运（表述修正）：

| 旧页 | 命运 | 后果 |
|------|------|------|
| Page 54~60 | 位于新 App 区（4~60）内，升级擦写 App 时**物理抹除** | 参数丢失 |
| Page 61~63 | 物理存活，但**旧 append-log 格式对新固件不可读** | 等效丢失 |

**全部 10 组参数在首次升级时都依赖上位机备份**，其中标定类数据（后果严重）必须逐寄存器核对。完整备份/恢复清单（Modbus，FC03 读 / FC06 写）：

| 参数组 | 寄存器范围 |
|--------|-----------|
| DAC 零点/满度 | 40021~40022 |
| 流量/累积单位 | 40023~40024 |
| 仪表系数/介质系数 | 40025~40028 |
| 小信号切除 | 40029~40030 |
| 量程低/高（SpanLo/SpanHi，2×float=4 寄存器） | **40031~40034** |
| 扩展配置参数 | 40070~40091 |
| 标定使能/系数/百分比 | 40096~40123 |
| 滤波窗口/采样间隔 | 40125~40126 |

工具实现注意：
- float 参数 FC06 写入须**低字先行**（先写偶数地址低字缓存，高字到达触发 setter）
- 恢复 40092（地址）/40093（波特率）后通信参数即刻变更，工具须按新参数重连
- 设备地址可配（1~247，默认 2），工具需支持指定地址/波特率或自动扫描
- 验收标准（P3）: 备份 JSON 与恢复后 Modbus 回读**逐寄存器比对一致**（含 SpanHi），作为量化验收项

## 5. Bootloader 设计（4KB，寄存器级，零 HAL 依赖）

### 5.1 设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 实现层次 | 寄存器级（不链接 HAL） | HAL 版需 ~7KB，超 4KB 预算；BL 逻辑简单、写完基本不动 |
| 传输接口 | USART1，115200-8N1 | 已有物理链路；BL 期间 UFL-1A 为被动采样无干扰（需确认引出点拓扑，见 §12-1） |
| 传输协议 | XModem-CRC（128B 包） | 协议极简，上位机实现成本低；每包 CRC16 校验 |
| **XModem 载荷** | **仅 App 切片（merged.bin 去掉前 4KB）** | 避免 BL 代码被写回 App 向量表区；img_size/CRC32 均按 App-only 定义 |
| 升级触发 | RAM 魔数 + 按键 + 无效镜像兜底 | 见 5.3 |
| 防砖策略 | 固件头最后写 + 跳转前校验 | 见 5.5 |
| BL 内反馈 | PB5 电源 LED 闪烁（快闪=升级中，长亮=完成） | 零成本，无 OLED |
| 看门狗 | BL 不启动 IWDG，但**主循环防御性喂狗** | 见 5.8，防"硬件看门狗"选项字节场景 |
| 编译约束 | **BL 全工程零 float** | 软浮点库一旦被引用 +2~3KB，直接击穿预算；CRC/时序均用整数运算 |

### 5.2 代码预算分解（4KB 上限）

| 模块 | 预算 | 说明 |
|------|------|------|
| 向量表 | 0.3KB | .intvec 实测 0xEC = 236B |
| 启动 + 时钟 | **0.6~0.8KB** | cstartup_M 28B + data_init 40B + zero_init 56B + ABImemcpy 166B + ABImemset 102B + SystemInit（FLASH->ACR 等待周期 + PLL 72MHz，~15 行） |
| Flash 驱动 | 0.3KB | 页擦除 + 半字/字编程（FLASH->CR 寄存器操作） |
| UART 驱动 | 0.2KB | GPIOA 时钟 + PA9/PA10 配置 + USART1 收发字节 |
| XModem-CRC 状态机 | 0.5KB | SOH/EOT/CAN 处理、重传、超时 |
| CRC32（查表免） | 0.3KB | 位运算实现，无 1KB 查找表 |
| 跳转/校验/主逻辑 | 0.4KB | header 校验、魔数处理、LED 指示 |
| **合计** | **~2.6~2.8KB** | 上限 4KB 内余量 ~1.2KB；P1 设 ≤4096B 编译门禁 |

**超预算预案（P1 实测触发）**: 砍 XModem 重传轮次 → CRC32 改查表外复用 → 最后手段接受 5KB BL 并缩减 App 区（需重走本方案分区评审）。

### 5.3 进入升级模式的条件（按优先级）

1. **RAM 魔数**: App 收到升级命令（菜单项或 Modbus 寄存器 40127）→ 写魔数 `0x554D4631` + 取反副本到固定地址 `0x20002000` → `NVIC_SystemReset()`。BL 启动后校验魔数对，命中则清除并进入升级模式
   - 地址实测数据: App 的 CSTACK 位于 0x20000D58~0x20001557（`UMF.map:457-459`），rw 止于 0x20000D57；0x20002000 位于未用区 0x20001558~0x20004FFF 内，下侧裕量 2,728B / 上侧 12,288B。构建期加 post-link 断言（`CSTACK$$Limit < 0x20002000`）防止未来栈增长侵入
2. **按键恢复**: 复位瞬间检测 PC15(K1) 若**已为低电平**则计时确认 ≥2s → 进入升级模式（防砖兜底）。正常开机时 PC15 为高，**不产生任何启动延迟**；需确认按键默认电平（前置确认项 #2）
3. **App 无效**: 固件头魔数校验失败 → 走后备校验（见 5.6），仍失败 → 停留 BL 等待升级（LED 慢闪提示）
4. **超时回退**: BL 进入升级模式后，**总等待窗口 10s**：窗口内每 1s 发一次 'C' 轮询（XModem-CRC 发起符），收到数据进入传输；窗口耗尽 → App 有效则跳转，无效则继续等待

### 5.4 升级流程与 XModem 时序约束

```
复位 → [魔数/按键判定] → 升级模式:
  初始化时钟/UART1/LED
  → 10s 窗口内发 'C' 轮询
  → 每 128B 包: CRC16 校验 → 按页擦除（首次写该页时）→ 字编程 → 完成后才回 ACK
  → EOT → 全镜像 CRC32 校验（头字段按 0 跳过）→ 写固件头（最后一步）→ LED 长亮 → 跳转 App
任何一步失败 → 从头重来；App 区擦除多少无所谓，BL 永远完好
```

**ACK 时序硬约束（必须写入状态机实现）**: 必须先收满 128B 再擦/写，且**编程完成后才发 ACK**——发送方等待 ACK 期间不发送数据，F1 擦除（≤40ms）/编程（≤4.5ms）期间的取指停顿不会丢字节。

### 5.5 固件镜像头与防砖设计

头固定位于 `App + 0x200`（0x08001200）。**该区域由 App 链接脚本保留**（修正审查 C1 缺陷——原方案"合并脚本注入、App 无需感知"会覆盖 .text 区已排列的代码）：

```c
/* App ICF 增加（stm32f103xb_flash.icf）: */
place at address mem:0x08001200 { readonly section .fw_header };

/* App 代码内定义空 section（boot_flag.c）: */
#pragma location = ".fw_header"
__root const uint8_t fw_header_reserved[24] = {0xFF};
```

合并脚本 `merge_fw.py` 将头**写入该保留区**（覆盖 0xFF 占位），CRC 在注入后计算。头结构：

```c
struct fw_header {                  /* 24B，位于 App 镜像偏移 0x200 */
    uint32_t magic;                 /* "UMFH" 0x554D4648 */
    uint16_t hw_id;                 /* 0x0103 = STM32F103C8 */
    uint16_t bl_min_ver;            /* BL 兼容最低版本 */
    uint32_t app_ver;               /* 如 0x010203 = v1.2.3 */
    uint32_t img_size;              /* App-only 镜像字节数（不含 BL 前 4KB） */
    uint32_t crc32;                 /* App-only 镜像 CRC32（头字段按 0 计算） */
    uint32_t reserved;
};
```

- **防砖核心**: 头最后写。升级中途断电 → 头区为 0xFF → BL 校验失败 → 停留 BL → 可重新升级。头存在 ⇒ 所有数据页已完整编程
- **CRC 零跳变（BL 伪代码必须显式体现）**: 升级完成校验 CRC 时，Flash 中头区尚为 0xFF，CRC 计算须按"头字段置 0"逐字节跳过该 24B，否则最后一步永远失败
- **跳转前校验**: BL 只校验 magic + img_size 范围（O(1)，上电零延迟）；CRC32 仅在升级完成时校验一次（位运算 ~0.1s，可接受）

### 5.6 跳转协议（BL → App）与后备校验

```c
__set_PRIMASK(1);                     /* 关全局中断 */
NVIC 清全部挂起;                       /* 防残留中断打到 App 的 ISR 表 */
SCB->VTOR = APP_BASE;                 /* App SystemInit 会再设，双保险 */
__set_MSP(*(uint32_t*)APP_BASE);      /* 从 App 向量表取初始 SP */
(*(void(**)(void))(APP_BASE + 4))();  /* 跳 Reset_Handler，不再返回 */
```

BL 只配置了 GPIOA/USART1/FLASH/RCC，App 的 `HAL_Init()` + `MX_xxx_Init()` 会完整重建外设状态（USART1 2400-9E1 与 BL 的 115200-8N1 无残留问题）。

**后备校验（解决开发期 App-only 烧录无头问题，审查 M2）**: 固件头魔数无效时，改查"向量表健全性"——初始 SP 落在 RAM 范围内（0x20000000~0x20005000）且 Reset_Handler 地址落在 App 区（0x08001000~0x0800F400）→ 判定可跳转。同时开发流程要求 IAR 后置命令自动注入头（§7.2），双保险。

### 5.7 BL 工程文件结构

```
Bootloader/
  boot_main.c        /* 主逻辑: 条件判定/升级状态机/跳转 */
  boot_flash.c/h     /* 寄存器级页擦除/字编程/CRC32 */
  boot_uart.c/h      /* 寄存器级 UART1 收发 */
  boot_xmodem.c/h    /* XModem-CRC 接收状态机 */
EWARM/
  Bootloader.ewp     /* 新工程（含上述 4 个 .c + startup_stm32f103xb.s 复用） */
  stm32f103xb_bootloader.icf  /* ROM 0~0xFFF, RAM 全 20KB(实际 <1KB), 无堆 */
```

### 5.8 IWDG 防御（审查 M5）

默认选项字节下系统复位会停止 IWDG，"BL 不启动 IWDG"成立；但若量产编程器使能了"Hardware watchdog"选项字节，复位后 IWDG 自动重新使能 → BL 被 ~1s 周期连续复位，升级功能全废。防御（~10 行，零风险）：

```c
/* BL 主循环，间隔 <100ms: IWDG 未启用时该写入无害 */
IWDG->KR = 0xAAAA;
```

同时 App 侧在 `param_storage_init` 之前提前 `HAL_IWDG_Refresh` 一次。**前置确认项 #3: 量产编程器配置中 HW-Watchdog 选项字节必须为关闭**（与 RDP 状态一并核实）。

## 6. App 侧修改清单

| # | 文件 | 修改 | 性质 |
|---|------|------|------|
| 1 | `EWARM/stm32f103xb_flash.icf` | `intvec_start/ROM_start = 0x08001000`，`ROM_end = 0x0800F3FF`（Page 60 末）；新增 `place at address 0x08001200 { readonly section .fw_header }` | 必须 |
| 2 | `Core/Src/system_stm32f1xx.c` | 启用 `USER_VECT_TAB_ADDRESS` + `VECT_TAB_OFFSET = 0x1000` | 必须 |
| 3 | `BSP/eeprom.c/h` | 新增 `page_read/page_write`（整页 RMW），保留 HAL 原语 | P0 |
| 4 | `BSP/param_storage.c` | 页面宏改新 3 页布局，setter 走 RMW；**新增旧 append-log 格式启动迁移**（参照 :281-327 先例） | P0 |
| 5 | `BSP/bsp_usart.c` / `bsp_menu.c` / `main.h` | Span/DAC 页地址改新偏移；升级触发入口（菜单项 + Modbus 寄存器 40127 写魔数 → RAM 魔数 → 复位） | P0/P2 |
| 6 | `BSP/boot_flag.c/h`（新增） | RAM 魔数读写 API（固定地址 0x20002000，8B）+ `.fw_header` 保留 section | P2 |
| 7 | `EWARM/UMF.ewp` | **新增 boot_flag.c 的 `<file>` 条目**（CLAUDE.md 强制规则：IAR 不自动发现新文件） | P2 |
| 8 | `Core/Src/main.c` | 启动时清除 RAM 魔数残留；`param_storage_init` 前提前 `HAL_IWDG_Refresh`（配合 §5.8） | P2 |
| 9 | `Drivers/CMSIS/Src/main.c` 等陈旧副本 | 删除或标注"已废弃"（防 P0 迁移误扫） | P0 |
| 10 | `README.md` / `CLAUDE.md` | Flash 布局、资源预算、升级流程章节更新 | P4 |

## 7. 构建、烧录与升级工具链

### 7.1 工程组织与产物

- `Project.eww` 工作区加入两个工程：`Bootloader.ewp`（新）+ `UMF.ewp`（改 ICF）
- `tools/merge_fw.py`：解析 BL.hex + App.hex → 生成 `merged.bin/hex`，注入固件头到 App+0x200 保留区 + 计算 CRC32。**唯一产物入口**，量产与升级均用 merged 镜像
- **开发流程（修正审查 M2 矛盾）**: App 工程的 IAR 后置命令自动调用 merge_fw.py 注入头 → 开发者每次下载的 App 镜像都带头；叠加 BL 后备校验（§5.6）双保险。严禁"裸 App hex 直接下载"

### 7.2 烧录策略

| 场景 | 方式 |
|------|------|
| 开发调试 | IAR 分别下载：BL 烧一次不动；App 每次下载（**Flash 下载设置选"按段擦除"，禁用整片擦除**）。注意: IAR 工作区下载默认只烧**当前选中工程**——切工程烧录的防误操作流程写入 README |
| 量产 | ST-Link/J-Link 烧 merged.bin 一次成型 |

### 7.3 上位机升级工具（P3）

`tools/ufl_update.py`（Python，复用 serial-monitor 技能生态）：
1. 参数备份（Modbus 按 §4.4 清单逐寄存器读取 → JSON）
2. 触发升级（Modbus 写 40127，或提示用户按复位键）
3. XModem-CRC 发送 **App-only 切片**（merged.bin 去掉前 4KB；进度条 + 每包确认）
4. 升级后参数恢复（JSON → Modbus 写回，注意 §4.4 的 FC06 低字先行与 40092/40093 重连策略）
5. 恢复后逐寄存器回读比对，输出比对报告

### 7.4 现有 Skill 适配

- `.claude/skills/build-iar`：支持双工程依次编译 + 合并产物
- `.claude/skills/workflow`（编译→烧录→监控流水线）：烧录步骤适配两镜像策略

## 8. 实施阶段与验收标准

| 阶段 | 内容 | 验收 |
|------|------|------|
| **P0** EEPROM 重打包 | eeprom.c 新 API + 3 页布局 + 全部调用点迁移 + **旧格式启动迁移** | App 基址不变前提下：参数保存/读取、断电保持、Modbus 读写、**旧布局设备升级后参数正确迁移（迁移逻辑单测覆盖）** 全回归通过；编译大小无回退 |
| **P1** BL 框架 | Bootloader.ewp + 时钟/UART/跳转 + LED 指示 + IWDG 防御 | 上电正常跳转现有 App；按键可进入 BL；LED 状态正确；**BL ≤ 4096B 编译门禁**；零 float 约束检查 |
| **P2** App 重定位 | ICF + VTOR + 魔数 + 升级触发入口 + 头保留区 | App 在 0x08001000 正常运行，全部功能回归；IAR 后置命令注入头生效 |
| **P3** 升级链路 | XModem 接收 + merged 工具 + ufl_update.py | 完整升级闭环：触发→传输→校验→跳转新固件；备份/恢复逐寄存器比对一致（量化验收） |
| **P4** 加固与文档 | 异常注入验证 + README/CLAUDE.md | 见验证清单全绿 |

## 9. 验证清单

- [ ] 正常升级全流程（旧版本 → 新版本，版本号核对）
- [ ] 升级中途断电矩阵：擦除前/擦除中/编程中/CRC 失败/头未写 → 重上电均可重新升级，设备不砖
- [ ] 无效镜像（坏 magic/坏 CRC/超区大小）→ 拒绝跳转
- [ ] 开发期 App-only 下载（无头）→ 后备校验仍可跳转；带后置命令注入头 → 正常校验
- [ ] 无升级数据 10s 窗口超时 → 正常跳转 App
- [ ] 按键恢复模式可用（App 损坏场景模拟）；正常开机无启动延迟
- [ ] 升级后参数保持（P0 之后升级，EEPROM 页 61~63 不被触碰）
- [ ] 旧布局存量机升级：启动迁移正确（仪表系数不出现 0.001 静默错误）
- [ ] 首次升级参数备份/恢复：JSON 备份 → 升级 → 写回 → **逐寄存器回读比对一致（含 SpanHi）**
- [ ] 升级后 IWDG/DMA/Modbus/OLED/DAC 全功能回归
- [ ] BL 大小 ≤ 4096B；App ≤ 0xE400（57KB）编译门禁；BL 零 float
- [ ] ST-Link 按段擦除烧录 App 不伤 BL
- [ ] 升级期间 UFL-1A 模组无异常行为（回显干扰观察）
- [ ] 选项字节场景演练（若量产使能 HW-Watchdog）：BL 喂狗后升级流程可完成

## 10. 风险登记表

| 风险 | 影响 | 对策 |
|------|------|------|
| 升级断电 | App 损坏 | BL 常驻 + 头最后写 + 兜底校验；SWD（RDP 未启用）为最终恢复手段 |
| 首次升级丢标定数据 | 仪表失准 | 上位机备份/恢复流程（§4.4 全清单）；发布说明中强制要求先备份；**存量设备标定台账需确认（前置确认项 #4）** |
| P0 直接上线无迁移 | 仪表系数静默变 0.001 | §4.3 启动迁移强制实现，P0 验收含迁移单测 |
| USART1 (PA9/PA10) 未物理引出 / 与 UFL-1A 共线竞争 | 无法串口升级 / 升级数据冲突 | **硬件前置确认项 #1**；备选方案：USART2 RS-485 升级（BL 需加 PA1 DE 控制） |
| HW-Watchdog 选项字节已使能 | BL 复位循环，升级功能全废 | 前置确认 #3 + §5.8 防御性喂狗 |
| 按键默认电平假设错误 | 恢复模式失效 | P1 前实测 PC15 默认电平，调整判定极性 |
| BL 寄存器级实现出错 | 写坏 Flash | 只允许写 Page 4~60 区间（代码内硬编码边界检查）；P4 异常注入全覆盖 |
| ST-Link 整片擦除误伤 BL | 开发期砖机 | 烧录设置固化"按段擦除"；README 警示 + 工作区多工程默认行为说明 |
| RAM 魔数地址与栈/数据冲突 | 误触发/失效 | 固定 0x20002000（实测裕量下 2.7KB/上 12KB）；post-link 断言；App 上电即清除 |
| BL 超 4KB 预算 | 分区重排 | §5.2 预案三级；P1 门禁提前暴露 |
| 升级期间 4-20mA 输出掉零 / 流量测量中断 | 现场控制系统判断线、累积流量空洞 | **文档与升级流程明示**：升级期间模拟输出无效、测量中断，累积值不补记；现场必要时切旁路 |
| 公开仓库泄露敏感信息 | 安全 | 本方案无加密无密钥（YAGNI）；提交前按仓库规范扫描 |

## 11. 明确不做（YAGNI）

- ❌ 固件加密/签名校验（非安全敏感设备，RDP 未用，无密钥管理基础设施）
- ❌ A/B 双区冗余（单 Bank 64KB 无空间）
- ❌ BL 内 OLED 显示（8KB 驱动超预算，LED 指示足够）
- ❌ Modbus 协议内传输固件（复杂度高，XModem 更通用）
- ❌ EEPROM 页内双槽轮换磨损均衡（P0 后按实测磨损再评估）

## 12. 前置确认项（开工前必须落实，结论回填前 P1 不得启动）

1. **USART1 PA9/PA10 引出点拓扑**：是否在 PCB 上有引出；引出点与 UFL-1A 模组是否共线（共线则升级时模组 TX 与工具 TX 在 PA10 上竞争）
2. PC15 (K1) 按键默认电平与面板功能标识
3. **量产编程器选项字节配置**：HW-Watchdog 必须关闭、RDP 等级确认（当前代码无 RDP 使用）
4. 存量设备数量与出厂标定数据台账是否可追溯（不可追溯则首次升级备份流程无法补救标定损坏）
5. 是否接受"首次升级丢参数"（配合上位机备份恢复）的过渡方案
